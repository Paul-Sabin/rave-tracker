# Project Guidelines

## Environment

The app is deployed at `https://ravetracker.whotrustswho.com`. Use this URL in checkpoint verification steps — not `localhost:8080`.

## Checkpoints

Before presenting a human verification checkpoint, always commit and push all changes (`git push origin main`) so the deployed app reflects what's being verified.
