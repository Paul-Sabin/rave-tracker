---
phase: 07-password-management
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - ra-tracker/ra_tracker/web/templates/email/password_reset.html
  - ra-tracker/ra_tracker/services/email_sender.py
  - ra-tracker/ra_tracker/web/templates/password_reset_request.html
  - ra-tracker/ra_tracker/web/templates/password_reset_form.html
  - ra-tracker/ra_tracker/web/routes.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can request password reset by entering email"
    - "Reset email is sent with clickable link (if email exists)"
    - "Same success message shown whether email exists or not (no enumeration)"
    - "User can set new password via reset link"
    - "Expired/invalid tokens show error with request new link option"
    - "All sessions are invalidated after password reset"
    - "Password reset events appear in audit log"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/templates/email/password_reset.html"
      provides: "HTML email template with reset button and link"
      contains: "reset_url"
    - path: "ra-tracker/ra_tracker/web/templates/password_reset_request.html"
      provides: "Enter email form for forgot password"
      contains: "form.*forgot-password"
    - path: "ra-tracker/ra_tracker/web/templates/password_reset_form.html"
      provides: "Enter new password form"
      contains: "new_password"
    - path: "ra-tracker/ra_tracker/services/email_sender.py"
      provides: "send_password_reset_email function"
      exports: ["send_password_reset_email"]
    - path: "ra-tracker/ra_tracker/web/routes.py"
      provides: "Password reset routes"
      contains: ["forgot-password", "reset-password"]
  key_links:
    - from: "routes.py forgot-password POST"
      to: "send_password_reset_email"
      via: "async call after rate limit check"
      pattern: "await send_password_reset_email"
    - from: "routes.py reset-password POST"
      to: "db.delete_user_sessions"
      via: "session invalidation after password update"
      pattern: "delete_user_sessions.*user"
    - from: "password reset routes"
      to: "log_audit_event"
      via: "audit logging for all password events"
      pattern: "log_audit_event.*password"
---

<objective>
Implement password reset flow (forgot password -> email -> reset form)

Purpose: Allow users who forgot their password to securely reset it via email, meeting SEC-02, ACCT-01, ACCT-02, ACCT-03, AUDIT-04
Output: Email template, request/completion forms, routes with rate limiting and audit logging
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-password-management/07-CONTEXT.md
@.planning/phases/07-password-management/07-RESEARCH.md
@.planning/phases/07-password-management/07-01-SUMMARY.md

# Existing patterns
@ra-tracker/ra_tracker/web/templates/email/verification.html
@ra-tracker/ra_tracker/web/templates/login.html
@ra-tracker/ra_tracker/services/email_sender.py
@ra-tracker/ra_tracker/web/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create password reset email template and send function</name>
  <files>ra-tracker/ra_tracker/web/templates/email/password_reset.html, ra-tracker/ra_tracker/services/email_sender.py</files>
  <action>
Create password_reset.html email template following the exact pattern from verification.html:

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password</title>
</head>
<body style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px;">
  <div style="background: #f8f9fa; border-radius: 8px; padding: 30px; margin-bottom: 20px;">
    <h1 style="color: #1a1a1a; margin: 0 0 20px 0; font-size: 24px;">Reset your password</h1>

    <p style="margin: 0 0 20px 0;">Hi,</p>

    <p style="margin: 0 0 20px 0;">Click the button below to reset your password:</p>

    <div style="text-align: center; margin: 30px 0;">
      <a href="{{ reset_url }}"
         style="background: #2563eb; color: white; padding: 14px 28px; text-decoration: none; border-radius: 6px; display: inline-block; font-weight: 500;">
        Reset Password
      </a>
    </div>

    <p style="margin: 0 0 10px 0; font-size: 14px; color: #666;">
      Or copy and paste this link into your browser:
    </p>
    <p style="margin: 0 0 20px 0; font-size: 14px; word-break: break-all;">
      <a href="{{ reset_url }}" style="color: #2563eb;">{{ reset_url }}</a>
    </p>

    <p style="margin: 0; font-size: 14px; color: #666;">
      This link will expire in 24 hours.
    </p>
  </div>

  <p style="font-size: 12px; color: #999; margin: 0;">
    If you didn't request this, you can safely ignore this email. Your password won't change.
  </p>

  <hr style="border: none; border-top: 1px solid #eee; margin: 20px 0;">

  <p style="font-size: 12px; color: #999; margin: 0;">
    RA Tracker
  </p>
</body>
</html>
```

Add send_password_reset_email function to email_sender.py after send_verification_email:

```python
async def send_password_reset_email(
    user_email: str,
    user_id: int,
) -> bool:
    """Send password reset email with secure token link.

    Args:
        user_email: Recipient email address
        user_id: User ID for token generation

    Returns:
        True if sent successfully, False otherwise
    """
    conf = _get_email_config()
    if not conf:
        logger.warning("Email not configured, skipping reset email send")
        return False

    # Import here to avoid circular imports
    from ..web.password_reset import generate_reset_token

    config = get_config()
    token = generate_reset_token(user_id)
    reset_url = f"{config.app.base_url}/reset-password/{token}"

    message = MessageSchema(
        subject="Reset your password",  # Simple, not branded per CONTEXT.md
        recipients=[user_email],
        template_body={
            "reset_url": reset_url,
        },
        subtype=MessageType.html,
    )

    try:
        fm = FastMail(conf)
        await fm.send_message(message, template_name="password_reset.html")
        logger.info(f"Sent password reset email to {user_email}")
        return True
    except Exception as e:
        logger.error(f"Failed to send reset email to {user_email}: {e}")
        return False
```
  </action>
  <verify>
```bash
# Check template exists
ls ra-tracker/ra_tracker/web/templates/email/password_reset.html

# Check function added (import test)
python -c "from ra_tracker.services.email_sender import send_password_reset_email; print('OK')"
```
  </verify>
  <done>Email template exists with reset_url placeholder, send_password_reset_email function importable</done>
</task>

<task type="auto">
  <name>Task 2: Create password reset request and completion templates</name>
  <files>ra-tracker/ra_tracker/web/templates/password_reset_request.html, ra-tracker/ra_tracker/web/templates/password_reset_form.html</files>
  <action>
Create password_reset_request.html (forgot password - enter email):

```html
{% extends "base.html" %}

{% block title %}Forgot Password - RA Tracker{% endblock %}

{% block content %}
<div class="auth-container">
    <h1>Reset your password</h1>
    <p class="text-muted mb-2">Enter your email address and we'll send you a link to reset your password.</p>

    {% if error %}
    <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    {% if success %}
    <div class="alert alert-success">{{ success }}</div>
    {% else %}
    <form action="/forgot-password" method="post">
        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">

        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" class="form-control"
                   required autofocus placeholder="you@example.com">
        </div>

        <button type="submit" class="btn btn-primary btn-block">Send Reset Link</button>
    </form>
    {% endif %}

    <p class="text-center mt-2">
        <a href="/login">Back to login</a>
    </p>
</div>
{% endblock %}
```

Create password_reset_form.html (enter new password):

```html
{% extends "base.html" %}

{% block title %}Set New Password - RA Tracker{% endblock %}

{% block content %}
<div class="auth-container">
    <h1>Set new password</h1>

    {% if error %}
    <div class="alert alert-danger">
        {{ error }}
        {% if show_request_new %}
        <div class="mt-1">
            <a href="/forgot-password" class="btn btn-secondary btn-sm">Request new link</a>
        </div>
        {% endif %}
    </div>
    {% endif %}

    {% if not error or token %}
    <form action="/reset-password/{{ token }}" method="post">
        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">

        <div class="form-group">
            <label for="new_password">New Password</label>
            <div class="password-input-wrapper">
                <input type="password" id="new_password" name="new_password"
                       class="form-control" required minlength="8"
                       placeholder="At least 8 characters">
                <button type="button" class="password-toggle" onclick="togglePasswordVisibility()"
                        aria-label="Show password">
                    <svg id="eye-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                        <circle cx="12" cy="12" r="3"></circle>
                    </svg>
                </button>
            </div>
            <!-- Strength meter -->
            <div class="strength-meter mt-1">
                <div id="strength-bar" class="strength-bar"></div>
            </div>
            <p id="strength-text" class="strength-text"></p>
        </div>

        <button type="submit" class="btn btn-primary btn-block">Set New Password</button>
    </form>
    {% endif %}
</div>

<style>
.password-input-wrapper {
    position: relative;
}
.password-toggle {
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    cursor: pointer;
    padding: 5px;
    color: #666;
}
.password-toggle:hover {
    color: #333;
}
.strength-meter {
    height: 4px;
    background: #e9ecef;
    border-radius: 2px;
    overflow: hidden;
}
.strength-bar {
    height: 100%;
    width: 0;
    transition: width 0.2s, background-color 0.2s;
    border-radius: 2px;
}
.strength-text {
    font-size: 0.85rem;
    margin-top: 0.25rem;
    min-height: 1.2em;
}
</style>

<script src="https://cdnjs.cloudflare.com/ajax/libs/zxcvbn/4.4.2/zxcvbn.js"></script>
<script>
function togglePasswordVisibility() {
    const input = document.getElementById('new_password');
    const icon = document.getElementById('eye-icon');
    if (input.type === 'password') {
        input.type = 'text';
        icon.innerHTML = '<path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line>';
    } else {
        input.type = 'password';
        icon.innerHTML = '<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle>';
    }
}

document.getElementById('new_password').addEventListener('input', function(e) {
    const password = e.target.value;
    if (!password) {
        document.getElementById('strength-bar').style.width = '0';
        document.getElementById('strength-text').textContent = '';
        return;
    }

    const result = zxcvbn(password);
    const strengthBar = document.getElementById('strength-bar');
    const strengthText = document.getElementById('strength-text');

    const colors = ['#ef4444', '#f59e0b', '#f59e0b', '#22c55e', '#22c55e'];
    const labels = ['Very weak', 'Weak', 'Fair', 'Strong', 'Very strong'];
    const widths = ['20%', '40%', '60%', '80%', '100%'];

    strengthBar.style.width = widths[result.score];
    strengthBar.style.backgroundColor = colors[result.score];
    strengthText.textContent = labels[result.score];
    strengthText.style.color = colors[result.score];

    if (result.score <= 2 && result.feedback.warning) {
        strengthText.textContent += ': ' + result.feedback.warning;
    }
});
</script>
{% endblock %}
```
  </action>
  <verify>
```bash
ls ra-tracker/ra_tracker/web/templates/password_reset_request.html
ls ra-tracker/ra_tracker/web/templates/password_reset_form.html
```
  </verify>
  <done>Both templates exist with proper forms, CSRF tokens, and zxcvbn strength meter</done>
</task>

<task type="auto">
  <name>Task 3: Add password reset routes with rate limiting and audit logging</name>
  <files>ra-tracker/ra_tracker/web/routes.py</files>
  <action>
Add imports at top of routes.py:
```python
from .password_reset import generate_reset_token, verify_reset_token
from .password_validation import validate_password
from .rate_limit import reset_limiter
from ..services.email_sender import send_password_reset_email
```

Add routes after the verification routes (around line 350-400). Add all 4 routes:

1. GET /forgot-password - Show request form
```python
@router.get("/forgot-password", response_class=HTMLResponse)
async def forgot_password_form(request: Request):
    """Show password reset request form."""
    templates = get_templates(request)
    return templates.TemplateResponse("password_reset_request.html", {
        "request": request,
        "csrf_token": getattr(request.state, 'csrf_token', ''),
    })
```

2. POST /forgot-password - Process reset request
```python
@router.post("/forgot-password", response_class=HTMLResponse)
async def request_password_reset(
    request: Request,
    email: str = Form(...),
):
    """Process password reset request."""
    templates = get_templates(request)
    db = get_db()

    # Normalize email
    email = email.lower().strip()

    # Check rate limit BEFORE looking up user (timing attack prevention)
    allowed, reason = reset_limiter.check_rate_limit(email)
    if not allowed:
        log_audit_event("password.reset_rate_limited", request,
                       details={"email_hash": hashlib.sha256(email.encode()).hexdigest()[:16]})
        return templates.TemplateResponse("password_reset_request.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "error": "Too many requests. Try again later.",
        })

    # Record request for rate limiting (regardless of user existence)
    reset_limiter.record_request(email)

    # Look up user
    user = db.get_user_by_email(email)

    # Always show success (don't reveal if email exists)
    if user:
        await send_password_reset_email(user.email, user.id)
        log_audit_event("password.reset_requested", request, user_id=user.id)
    else:
        log_audit_event("password.reset_unknown_email", request,
                       details={"email_hash": hashlib.sha256(email.encode()).hexdigest()[:16]})

    return templates.TemplateResponse("password_reset_request.html", {
        "request": request,
        "csrf_token": getattr(request.state, 'csrf_token', ''),
        "success": "If an account exists with that email, we've sent a reset link.",
    })
```

3. GET /reset-password/{token} - Show new password form
```python
@router.get("/reset-password/{token}", response_class=HTMLResponse)
async def reset_password_form(request: Request, token: str):
    """Show password reset form."""
    templates = get_templates(request)

    # Validate token early to show appropriate message
    try:
        verify_reset_token(token)
    except SignatureExpired:
        return templates.TemplateResponse("password_reset_form.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "error": "This reset link has expired.",
            "show_request_new": True,
        })
    except BadSignature:
        return templates.TemplateResponse("password_reset_form.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "error": "Invalid reset link.",
        })

    return templates.TemplateResponse("password_reset_form.html", {
        "request": request,
        "csrf_token": getattr(request.state, 'csrf_token', ''),
        "token": token,
    })
```

4. POST /reset-password/{token} - Process password reset
```python
@router.post("/reset-password/{token}", response_class=HTMLResponse)
async def complete_password_reset(
    request: Request,
    token: str,
    new_password: str = Form(...),
):
    """Complete password reset with new password."""
    templates = get_templates(request)
    db = get_db()

    # Validate token
    try:
        data = verify_reset_token(token)
        user_id = data["user_id"]
    except SignatureExpired:
        return templates.TemplateResponse("password_reset_form.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "error": "This reset link has expired.",
            "show_request_new": True,
        })
    except BadSignature:
        return templates.TemplateResponse("password_reset_form.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "error": "Invalid reset link.",
        })

    # Validate password
    is_valid, error_msg = validate_password(new_password)
    if not is_valid:
        return templates.TemplateResponse("password_reset_form.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "token": token,
            "error": error_msg,
        })

    # Get user
    user = db.get_user_by_id(user_id)
    if not user:
        return templates.TemplateResponse("password_reset_form.html", {
            "request": request,
            "csrf_token": getattr(request.state, 'csrf_token', ''),
            "error": "User not found.",
        })

    # Update password
    from argon2 import PasswordHasher
    hasher = PasswordHasher()
    new_hash = hasher.hash(new_password)
    db.update_user_password_hash(user_id, new_hash)

    # CRITICAL: Invalidate ALL sessions (per CONTEXT.md - assume password was compromised)
    db.delete_user_sessions(user_id)

    log_audit_event("password.reset_completed", request, user_id=user_id)

    # Redirect to login with success message
    return RedirectResponse(
        url="/login?message=Password+updated.+Please+log+in.",
        status_code=303,
    )
```

Note: The login route should display the message query param. Check if login.html already handles this, otherwise add display logic.
  </action>
  <verify>
```bash
# Start the app and test routes
cd ra-tracker && python -m ra_tracker.main &
sleep 2

# Test forgot password form loads
curl -s http://localhost:8000/forgot-password | grep -o "Reset your password"

# Test invalid token shows error
curl -s http://localhost:8000/reset-password/invalid-token | grep -o "Invalid reset link"

pkill -f "python -m ra_tracker.main"
```
  </verify>
  <done>All 4 routes exist and handle success/error cases, rate limiting applied, sessions invalidated on reset</done>
</task>

</tasks>

<verification>
Complete reset flow works:
1. Visit /forgot-password, submit email
2. Check that success message appears (regardless of email existence)
3. Verify rate limiting blocks after 3 requests per hour
4. With valid token from email, reset form shows with strength meter
5. Submitting valid password updates it and redirects to login
6. All sessions for user are invalidated (check sessions table is empty for that user)
7. Audit log contains password.reset_requested and password.reset_completed events
</verification>

<success_criteria>
1. /forgot-password shows form, POST always shows "If account exists..." success message
2. Rate limit error after 3 requests per hour
3. Reset email sent with clickable link (when user exists)
4. /reset-password/{token} validates token, shows strength meter
5. Weak/common passwords rejected with clear error
6. Successful reset invalidates all sessions and redirects to login
7. Audit events: password.reset_requested, password.reset_completed, password.reset_rate_limited
</success_criteria>

<output>
After completion, create `.planning/phases/07-password-management/07-02-SUMMARY.md`
</output>
