---
phase: 03-multi-tenant-access
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - ra-tracker/ra_tracker/web/auth.py
  - ra-tracker/ra_tracker/web/admin.py
  - ra-tracker/ra_tracker/web/templates/admin/rules.html
  - ra-tracker/ra_tracker/web/templates/admin/users.html
  - ra-tracker/ra_tracker/web/templates/base.html
  - ra-tracker/ra_tracker/web/app.py
  - ra-tracker/ra_tracker/database.py
autonomous: true

must_haves:
  truths:
    - "Admin can view all users' rules (read-only)"
    - "Admin can view list of registered users"
    - "Non-admin cannot access /admin/* routes"
    - "Admin link appears in navigation only for admins"
    - "Admin views show owner information for each rule"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/auth.py"
      provides: "require_admin dependency"
      exports: ["require_admin"]
    - path: "ra-tracker/ra_tracker/web/admin.py"
      provides: "Admin routes"
      exports: ["admin_router"]
    - path: "ra-tracker/ra_tracker/web/templates/admin/rules.html"
      provides: "Admin rules view template"
      contains: "owner_name"
    - path: "ra-tracker/ra_tracker/web/templates/admin/users.html"
      provides: "Admin users list template"
      contains: "user.email"
    - path: "ra-tracker/ra_tracker/web/templates/base.html"
      provides: "Conditional admin link"
      contains: "user.is_admin"
  key_links:
    - from: "admin.py"
      to: "require_admin"
      via: "Depends(require_admin)"
      pattern: "Depends\\(require_admin\\)"
    - from: "app.py"
      to: "admin_router"
      via: "include_router"
      pattern: "include_router\\(admin_router\\)"
    - from: "base.html"
      to: "/admin/rules"
      via: "conditional link"
      pattern: "user\\.is_admin.*admin"
---

<objective>
Create admin routes and templates for oversight capabilities

Purpose: Allow admin users to view all users' rules (read-only) and see a list of registered users, per the context decisions. Admin functionality is separate from regular user navigation.

Output: Admin router with /admin/rules and /admin/users routes, read-only templates, and conditional navigation link.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-tenant-access/03-RESEARCH.md
@.planning/phases/03-multi-tenant-access/03-01-SUMMARY.md

# Source files
@ra-tracker/ra_tracker/web/auth.py
@ra-tracker/ra_tracker/web/app.py
@ra-tracker/ra_tracker/web/templates/base.html
@ra-tracker/ra_tracker/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add require_admin dependency and admin database methods</name>
  <files>ra-tracker/ra_tracker/web/auth.py, ra-tracker/ra_tracker/database.py</files>
  <action>
**In auth.py:**

Add `require_admin` dependency after `require_auth`:

```python
async def require_admin(user: User = Depends(require_auth)) -> User:
    """Require admin privileges. Returns 403 Forbidden for non-admins."""
    if not user.is_admin:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="Admin access required"
        )
    return user
```

Import HTTPException and status if not already imported.

**In database.py:**

Add two new methods for admin views:

1. **get_all_rules_with_users() -> List[dict]:**
   - Returns all rules with owner display_name and email
   - Uses LEFT JOIN to handle rules without owner (shouldn't happen, but defensive)
   - Order by owner display_name, then rule_type, then target_name
   ```sql
   SELECT r.*, u.display_name as owner_name, u.email as owner_email
   FROM rules r
   LEFT JOIN users u ON r.user_id = u.id
   ORDER BY u.display_name, r.rule_type, r.target_name
   ```
   - Returns list of dicts (not Rule objects) since we're adding owner info

2. **get_all_users() -> List[User]:**
   - Returns all registered users
   - Order by created_at DESC (newest first)
   - For admin user list view
   ```sql
   SELECT * FROM users ORDER BY created_at DESC
   ```
  </action>
  <verify>
1. `python -c "from ra_tracker.web.auth import require_admin; print('require_admin exists')"`
2. `python -c "from ra_tracker.database import Database; d = Database(':memory:'); d.init_schema(); print(hasattr(d, 'get_all_rules_with_users'), hasattr(d, 'get_all_users'))"`
  </verify>
  <done>
- require_admin dependency returns 403 for non-admin users
- get_all_rules_with_users returns rules with owner info
- get_all_users returns all registered users
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin router and templates</name>
  <files>ra-tracker/ra_tracker/web/admin.py, ra-tracker/ra_tracker/web/templates/admin/rules.html, ra-tracker/ra_tracker/web/templates/admin/users.html, ra-tracker/ra_tracker/web/app.py, ra-tracker/ra_tracker/web/templates/base.html</files>
  <action>
**Create admin.py:**

New file at `ra-tracker/ra_tracker/web/admin.py`:

```python
"""Admin routes for RA Tracker - view-only oversight."""

from fastapi import APIRouter, Request, Depends
from fastapi.responses import HTMLResponse

from ..database import get_db, User
from .auth import require_admin

admin_router = APIRouter(prefix="/admin", tags=["admin"])


def get_templates(request: Request):
    """Get templates from app state."""
    return request.app.state.templates


@admin_router.get("/rules", response_class=HTMLResponse)
async def admin_rules(request: Request, user: User = Depends(require_admin)):
    """Admin view: All users' rules (read-only)."""
    templates = get_templates(request)
    db = get_db()

    all_rules = db.get_all_rules_with_users()

    # Group rules by owner for display
    rules_by_user = {}
    for rule in all_rules:
        owner = rule.get("owner_name") or "Unknown"
        if owner not in rules_by_user:
            rules_by_user[owner] = []
        rules_by_user[owner].append(rule)

    return templates.TemplateResponse(
        "admin/rules.html",
        {
            "request": request,
            "user": user,
            "all_rules": all_rules,
            "rules_by_user": rules_by_user,
            "total_rules": len(all_rules),
        },
    )


@admin_router.get("/users", response_class=HTMLResponse)
async def admin_users(request: Request, user: User = Depends(require_admin)):
    """Admin view: All registered users."""
    templates = get_templates(request)
    db = get_db()

    all_users = db.get_all_users()

    return templates.TemplateResponse(
        "admin/users.html",
        {
            "request": request,
            "user": user,
            "all_users": all_users,
            "total_users": len(all_users),
        },
    )
```

**Create templates/admin/ directory and rules.html:**

```html
{% extends "base.html" %}

{% block title %}Admin - All Rules{% endblock %}

{% block content %}
<div class="max-w-4xl mx-auto px-4 py-6">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Admin: All Rules</h1>
    <p class="text-muted">View-only list of all users' tracking rules ({{ total_rules }} total)</p>
  </div>

  <div class="flex gap-4 mb-6">
    <a href="/admin/rules" class="px-4 py-2 bg-primary text-white rounded">Rules</a>
    <a href="/admin/users" class="px-4 py-2 bg-border rounded hover:bg-muted">Users</a>
  </div>

  {% for owner, rules in rules_by_user.items() %}
  <div class="mb-6">
    <h2 class="text-lg font-semibold mb-2 border-b pb-2">{{ owner }} ({{ rules|length }} rules)</h2>
    <div class="space-y-2">
      {% for rule in rules %}
      <div class="p-3 bg-card rounded border flex justify-between items-center">
        <div>
          <span class="font-medium">{{ rule.target_name }}</span>
          <span class="text-sm text-muted ml-2">({{ rule.rule_type }})</span>
        </div>
        <div class="flex items-center gap-2 text-sm">
          <span class="{% if rule.is_active %}text-green-600{% else %}text-red-600{% endif %}">
            {{ 'Active' if rule.is_active else 'Inactive' }}
          </span>
          <span class="text-muted">{{ rule.notify_mode }}</span>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
  {% else %}
  <p class="text-muted">No rules found.</p>
  {% endfor %}
</div>
{% endblock %}
```

**Create templates/admin/users.html:**

```html
{% extends "base.html" %}

{% block title %}Admin - Users{% endblock %}

{% block content %}
<div class="max-w-4xl mx-auto px-4 py-6">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Admin: Users</h1>
    <p class="text-muted">Registered users ({{ total_users }} total)</p>
  </div>

  <div class="flex gap-4 mb-6">
    <a href="/admin/rules" class="px-4 py-2 bg-border rounded hover:bg-muted">Rules</a>
    <a href="/admin/users" class="px-4 py-2 bg-primary text-white rounded">Users</a>
  </div>

  <div class="space-y-2">
    {% for u in all_users %}
    <div class="p-4 bg-card rounded border">
      <div class="flex justify-between items-start">
        <div>
          <p class="font-medium">{{ u.display_name }}</p>
          <p class="text-sm text-muted">{{ u.email }}</p>
        </div>
        <div class="text-right text-sm">
          {% if u.is_admin %}<span class="bg-purple-100 text-purple-800 px-2 py-1 rounded text-xs">Admin</span>{% endif %}
          <p class="text-muted mt-1">Joined {{ u.created_at.strftime('%Y-%m-%d') if u.created_at else 'Unknown' }}</p>
        </div>
      </div>
    </div>
    {% else %}
    <p class="text-muted">No users found.</p>
    {% endfor %}
  </div>
</div>
{% endblock %}
```

**Update app.py to include admin_router:**

Add import and include_router after the existing router:

```python
from .admin import admin_router

# In the create_app function or wherever routers are added:
app.include_router(admin_router)
```

**Update base.html to add conditional admin link:**

In the navigation section, add an Admin link that only shows for admins. Place it after Settings but before Logout:

```html
{% if user and user.is_admin %}
<a href="/admin/rules" class="px-4 py-2 min-h-11 flex items-center rounded hover:bg-border transition-colors {% if request.url.path.startswith('/admin') %}bg-border{% endif %}">
    Admin
</a>
{% endif %}
```

Add same link to mobile menu if present.
  </action>
  <verify>
1. Start app as admin user
2. Admin link should appear in navigation
3. /admin/rules should show all users' rules grouped by owner
4. /admin/users should show all registered users
5. Login as non-admin user
6. Admin link should NOT appear
7. Navigating to /admin/rules directly should return 403
  </verify>
  <done>
- admin.py creates /admin/rules and /admin/users routes
- Admin templates show read-only data with owner info
- Admin link appears only for admin users
- Non-admin users get 403 on admin routes
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Start app: `cd ra-tracker && python -m ra_tracker`
2. Login as admin (first user)
3. Verify Admin link appears in nav
4. Visit /admin/rules - see all rules with owner names
5. Visit /admin/users - see all users
6. Create second non-admin user
7. Login as non-admin user
8. Verify Admin link does NOT appear
9. Try /admin/rules directly - should get 403 Forbidden
</verification>

<success_criteria>
1. require_admin dependency exists and returns 403 for non-admins
2. /admin/rules shows all users' rules grouped by owner (read-only)
3. /admin/users shows all registered users
4. Admin link appears only for users with is_admin=True
5. Non-admin users cannot access /admin/* routes
6. Templates use Tailwind CSS consistent with existing UI
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-tenant-access/03-03-SUMMARY.md`
</output>
