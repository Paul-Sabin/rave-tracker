---
phase: 02-authentication
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - ra-tracker/ra_tracker/web/templates/dashboard.html
  - ra-tracker/ra_tracker/web/templates/rules.html
  - ra-tracker/ra_tracker/web/templates/settings.html
autonomous: false

must_haves:
  truths:
    - "Dashboard filter buttons have 44px minimum tap targets"
    - "Rules page works on mobile (375px width)"
    - "Settings page works on mobile (375px width)"
    - "All pages have no horizontal scroll at 375px"
    - "User can complete registration-to-dashboard flow on mobile"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/templates/dashboard.html"
      provides: "Mobile-optimized filter buttons"
      contains: "min-height.*44px|min-h-11"
    - path: "ra-tracker/ra_tracker/web/templates/rules.html"
      provides: "Mobile-responsive rule items"
      contains: "flex-wrap|gap"
    - path: "ra-tracker/ra_tracker/web/templates/settings.html"
      provides: "Touch-friendly form controls"
      contains: "min-height.*44px|min-h-11"
  key_links: []
---

<objective>
Complete mobile responsiveness for existing templates and verify full auth flow.

Purpose: Dashboard filter buttons, rules page, and settings page need mobile-friendly adjustments. This plan updates the CSS in these templates for 44px touch targets and proper mobile layouts, then verifies the complete authentication flow works on mobile.

Output: All templates work at 375px width with adequate touch targets. Verification confirms the full flow: register -> login -> dashboard -> rules -> settings -> logout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-CONTEXT.md

# Prior plan context
@.planning/phases/02-authentication/02-03-SUMMARY.md

# Files to modify
@ra-tracker/ra_tracker/web/templates/dashboard.html
@ra-tracker/ra_tracker/web/templates/rules.html
@ra-tracker/ra_tracker/web/templates/settings.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard.html for mobile touch targets</name>
  <files>ra-tracker/ra_tracker/web/templates/dashboard.html</files>
  <action>
Update the filter buttons in dashboard.html to have adequate touch targets for mobile.

1. Find the `.filter-btn` CSS (around line 232-250) and update:

```css
.filter-btn {
    padding: 0.5rem 0.75rem;
    min-height: 44px;  /* ADD: WCAG touch target */
    font-size: 0.85rem;
    border: 1px solid var(--border);
    border-radius: 4px;
    background: var(--bg-dark);
    color: var(--text-muted);
    cursor: pointer;
    transition: all 0.15s;
    display: flex;        /* ADD */
    align-items: center;  /* ADD */
}
```

2. Update the header-actions for mobile stacking. Find `.header-actions` and update:

```css
.header-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
}

@media (max-width: 640px) {
    .header-actions {
        width: 100%;
        flex-direction: column;
        align-items: stretch;
    }
    .filter-toggles {
        flex-direction: column;
        width: 100%;
    }
    .filter-group {
        flex-wrap: wrap;
    }
}
```

3. Update event item for better mobile display. Find `.event-meta` and add mobile adjustment:

```css
@media (max-width: 480px) {
    .event-meta {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.25rem;
    }
    .event-header {
        flex-direction: column;
    }
    .event-badges {
        margin-top: 0.5rem;
    }
}
```

4. Update context menu items to have adequate touch targets:

```css
.context-menu-item {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 16px;  /* Increase from 8px 12px */
    min-height: 44px;    /* ADD */
    cursor: pointer;
    color: var(--text);
    font-size: 0.9rem;
    transition: background 0.1s;
}
```
  </action>
  <verify>
```bash
# Check min-height 44px is set for filter buttons
grep -q "min-height: 44px" ra-tracker/ra_tracker/web/templates/dashboard.html && echo "OK: Filter buttons have 44px"

# Check media query exists
grep -q "@media.*max-width" ra-tracker/ra_tracker/web/templates/dashboard.html && echo "OK: Media queries added"
```
  </verify>
  <done>Dashboard filter buttons have 44px min-height. Mobile layout stacks filter groups vertically. Event metadata stacks on narrow screens. Context menu items have adequate touch targets.</done>
</task>

<task type="auto">
  <name>Task 2: Update rules.html for mobile layout</name>
  <files>ra-tracker/ra_tracker/web/templates/rules.html</files>
  <action>
Update rules.html for mobile-friendly layout.

1. Update the tab buttons for adequate touch targets. Find `.tab-btn` and update:

```css
.tab-btn {
    padding: 0.75rem 1rem;
    min-height: 44px;
    border: 1px solid var(--border);
    background: var(--bg-dark);
    color: var(--text);
    border-radius: 4px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
}
```

2. Update rule items for mobile stacking. Replace `.rule-item` styles:

```css
.rule-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem;
    border-bottom: 1px solid var(--border);
    flex-wrap: wrap;
    gap: 0.75rem;
}
.rule-item:last-child {
    border-bottom: none;
}

@media (max-width: 640px) {
    .rule-item {
        flex-direction: column;
        align-items: stretch;
    }
    .rule-info {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.25rem;
    }
    .rule-actions {
        flex-wrap: wrap;
        justify-content: flex-start;
    }
}
```

3. Update notify toggle buttons for touch targets:

```css
.notify-btn {
    padding: 0.5rem 0.75rem;
    min-height: 36px;  /* Slightly smaller since in group */
    border: none;
    background: var(--bg-dark);
    color: var(--text-muted);
    font-size: 0.8rem;
    cursor: pointer;
    border-right: 1px solid var(--border);
}
```

4. Update search result items for touch:

```css
.search-result {
    padding: 0.75rem 1rem;
    min-height: 44px;
    border-bottom: 1px solid var(--border);
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
}
```

5. Update the search tabs container for mobile wrapping:

```css
.search-tabs {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
}
```
  </action>
  <verify>
```bash
# Check touch targets in rules
grep -q "min-height: 44px" ra-tracker/ra_tracker/web/templates/rules.html && echo "OK: Touch targets in rules"

# Check mobile media query
grep -q "@media.*max-width" ra-tracker/ra_tracker/web/templates/rules.html && echo "OK: Mobile styles in rules"
```
  </verify>
  <done>Rules page has 44px touch targets for tabs and search results. Rule items stack vertically on mobile. Action buttons wrap properly on narrow screens.</done>
</task>

<task type="auto">
  <name>Task 3: Update settings.html for mobile layout</name>
  <files>ra-tracker/ra_tracker/web/templates/settings.html</files>
  <action>
Update settings.html for mobile-friendly layout. The form controls inherit from base.html (.form-control already has 44px), but add any page-specific improvements.

1. Add or update the style block for area search results:

```css
<style>
#area-results {
    max-height: 200px;
    overflow-y: auto;
}
.area-result {
    padding: 0.75rem 1rem;
    min-height: 44px;
    cursor: pointer;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
}
.area-result:hover {
    background: var(--border);
}

/* Mobile table adjustment */
@media (max-width: 480px) {
    .table th,
    .table td {
        padding: 0.5rem;
        font-size: 0.9rem;
    }
}

/* Make test button full width on mobile */
@media (max-width: 640px) {
    #test-telegram {
        width: 100%;
    }
}
</style>
```

2. Update the setup guide list for better mobile readability (wrap in a responsive style):

The ordered list in the alert-info div should have enough padding and wrapping to work on mobile. The existing inline style `margin-left: 1.5rem;` is fine.

3. Ensure all buttons have proper sizing (the existing .btn class from base.html handles this).
  </action>
  <verify>
```bash
# Check area result has touch target
grep -q "min-height: 44px" ra-tracker/ra_tracker/web/templates/settings.html && echo "OK: Settings touch targets"
```
  </verify>
  <done>Settings page area search results have 44px touch targets. Table and buttons responsive on mobile. Form controls inherit 44px from base.html.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete authentication system with mobile-responsive UI:
- Session-based authentication (database-backed)
- Registration with privacy consent
- Login/logout flow
- Protected routes
- Mobile hamburger navigation
- Mobile-friendly touch targets on all pages
- Track artist buttons for mobile users
  </what-built>
  <how-to-verify>
Test the complete flow on mobile (or browser at 375px width):

1. **Registration flow:**
   - Visit http://localhost:8080/ (should redirect to /login)
   - Click "Register" link
   - Try to submit without checking consent - should show error
   - Click Privacy Policy link - should open in new tab
   - Fill form: email, password (8+ chars), display name
   - Check consent checkbox
   - Submit - should redirect to dashboard

2. **Dashboard on mobile:**
   - Hamburger menu visible (not desktop nav)
   - Tap hamburger - nav panel opens
   - Your display name shown in nav
   - Tap an event to expand
   - See "Track Artists" section with buttons
   - Filter buttons are easy to tap (44px height)
   - No horizontal scroll

3. **Rules page on mobile:**
   - Navigate to Rules via hamburger menu
   - Search tabs are tappable
   - Search for an artist, results are tappable
   - Existing rules display properly (stack on narrow screens)

4. **Settings page on mobile:**
   - Navigate to Settings via hamburger menu
   - Form inputs are easy to tap
   - Area search results are tappable
   - Save button works

5. **Logout:**
   - Open hamburger menu
   - Tap Logout
   - Should redirect to /login
   - Visiting / should redirect to /login

6. **Login flow:**
   - Enter credentials
   - Submit - should redirect to dashboard
   - Session persists on page refresh
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All templates have been updated for mobile. The checkpoint task will manually verify the complete flow.

Automated checks:
```bash
# Check all templates have touch target sizes
for f in dashboard rules settings; do
  grep -q "min-height.*44" ra-tracker/ra_tracker/web/templates/${f}.html && echo "OK: ${f}.html has touch targets"
done

# Check Tailwind is loaded
grep -q "tailwindcss" ra-tracker/ra_tracker/web/templates/base.html && echo "OK: Tailwind CDN loaded"

# Check hamburger menu exists
grep -q "mobile-nav" ra-tracker/ra_tracker/web/templates/base.html && echo "OK: Mobile nav exists"
```
</verification>

<success_criteria>
1. All filter buttons on dashboard have 44px min-height
2. All search/result items on rules page have 44px min-height
3. All area search results on settings have 44px min-height
4. Rule items stack vertically on mobile (<640px)
5. Event metadata stacks on very narrow screens (<480px)
6. No horizontal scroll on any page at 375px width
7. Complete auth flow works: register -> dashboard -> rules -> settings -> logout -> login
8. Session persists across page refresh
9. User verification confirms mobile experience is usable
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-05-SUMMARY.md`
</output>
