---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ra-tracker/ra_tracker/web/routes.py
  - ra-tracker/ra_tracker/web/templates/login.html
  - ra-tracker/ra_tracker/web/templates/register.html
  - ra-tracker/ra_tracker/web/templates/privacy.html
autonomous: true

must_haves:
  truths:
    - "User can register with email, password, display_name and consent checkbox"
    - "User can log in with email and password"
    - "User can log out"
    - "Invalid credentials show error message"
    - "Registration requires explicit consent (unticked checkbox)"
    - "Privacy Policy page accessible at /privacy"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/routes.py"
      provides: "Auth routes: GET/POST /login, /register, /logout, GET /privacy"
      contains: "@router.post(\"/login\")"
    - path: "ra-tracker/ra_tracker/web/templates/login.html"
      provides: "Login form with email/password"
      contains: "form.*login"
    - path: "ra-tracker/ra_tracker/web/templates/register.html"
      provides: "Registration form with consent checkbox"
      contains: "type=\"checkbox\".*consent"
    - path: "ra-tracker/ra_tracker/web/templates/privacy.html"
      provides: "Privacy Policy content"
      contains: "Privacy Policy"
  key_links:
    - from: "ra-tracker/ra_tracker/web/routes.py"
      to: "ra-tracker/ra_tracker/web/auth.py"
      via: "import create_user_session, set_session_cookie"
      pattern: "from.*auth import|create_user_session|set_session_cookie"
    - from: "ra-tracker/ra_tracker/web/routes.py"
      to: "ra-tracker/ra_tracker/database.py"
      via: "db.create_user, db.verify_password"
      pattern: "db\\.create_user|db\\.verify_password"
---

<objective>
Implement authentication routes (register, login, logout) and create auth-related templates.

Purpose: Users need to register, log in, and log out. This plan implements the core authentication endpoints and creates the login, register, and privacy policy templates. Templates use Tailwind CSS from the start (base.html migration is in Plan 03).

Output: Working auth routes with form handling, three new templates (login.html, register.html, privacy.html) with Tailwind styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-CONTEXT.md

# Prior plan context
@.planning/phases/02-authentication/02-01-SUMMARY.md

# Files to modify/create
@ra-tracker/ra_tracker/web/routes.py
@ra-tracker/ra_tracker/web/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add authentication routes to routes.py</name>
  <files>ra-tracker/ra_tracker/web/routes.py</files>
  <action>
Add authentication routes to the existing routes.py file:

1. Add imports at the top:
```python
import sqlite3
from ..database import get_db, Rule, User
from .auth import (
    create_user_session,
    set_session_cookie,
    clear_session_cookie,
    get_current_user,
)
```

2. Add GET /login route:
```python
@router.get("/login", response_class=HTMLResponse)
async def login_page(request: Request, user: Optional[User] = Depends(get_current_user)):
    """Login page. Redirect to dashboard if already logged in."""
    if user:
        return RedirectResponse(url="/", status_code=303)
    templates = get_templates(request)
    return templates.TemplateResponse("login.html", {"request": request})
```

3. Add POST /login route:
```python
@router.post("/login")
async def login(
    request: Request,
    email: str = Form(...),
    password: str = Form(...),
):
    """Process login form."""
    templates = get_templates(request)
    db = get_db()
    config = get_config()

    user = db.get_user_by_email(email)
    if not user:
        return templates.TemplateResponse("login.html", {
            "request": request,
            "error": "Invalid email or password",
            "email": email,
        })

    valid, new_hash = db.verify_password(user.password_hash, password)
    if not valid:
        return templates.TemplateResponse("login.html", {
            "request": request,
            "error": "Invalid email or password",
            "email": email,
        })

    # Rehash if needed (algorithm upgrade)
    if new_hash:
        db.update_user_password_hash(user.id, new_hash)

    # Create new session
    token, expires_at = create_user_session(user.id)

    response = RedirectResponse(url="/", status_code=303)
    set_session_cookie(response, token, expires_at, secure=config.session.secure_cookies)
    return response
```

4. Add GET /register route:
```python
@router.get("/register", response_class=HTMLResponse)
async def register_page(request: Request, user: Optional[User] = Depends(get_current_user)):
    """Registration page. Redirect to dashboard if already logged in."""
    if user:
        return RedirectResponse(url="/", status_code=303)
    templates = get_templates(request)
    return templates.TemplateResponse("register.html", {"request": request})
```

5. Add POST /register route:
```python
@router.post("/register")
async def register(
    request: Request,
    email: str = Form(...),
    password: str = Form(...),
    display_name: str = Form(...),
    consent: Optional[str] = Form(None),  # Checkbox value
):
    """Process registration form."""
    templates = get_templates(request)
    db = get_db()
    config = get_config()

    errors = {}

    # Validate consent
    if not consent:
        errors["consent"] = "You must agree to the Privacy Policy"

    # Validate password length
    if len(password) < 8:
        errors["password"] = "Password must be at least 8 characters"

    # Validate email format (basic check)
    if "@" not in email or "." not in email:
        errors["email"] = "Please enter a valid email address"

    if errors:
        return templates.TemplateResponse("register.html", {
            "request": request,
            "errors": errors,
            "email": email,
            "display_name": display_name,
        })

    try:
        user_id = db.create_user(email, password, display_name)
    except sqlite3.IntegrityError:
        return templates.TemplateResponse("register.html", {
            "request": request,
            "errors": {"email": "An account with this email already exists"},
            "email": email,
            "display_name": display_name,
        })

    # Auto-login after registration
    token, expires_at = create_user_session(user_id)

    response = RedirectResponse(url="/", status_code=303)
    set_session_cookie(response, token, expires_at, secure=config.session.secure_cookies)
    return response
```

6. Add POST /logout route:
```python
@router.post("/logout")
async def logout(request: Request):
    """Log out user."""
    db = get_db()
    token = request.cookies.get("session_token")

    if token:
        db.delete_session(token)

    response = RedirectResponse(url="/login", status_code=303)
    clear_session_cookie(response)
    return response
```

7. Add GET /privacy route:
```python
@router.get("/privacy", response_class=HTMLResponse)
async def privacy_page(request: Request):
    """Privacy Policy page."""
    templates = get_templates(request)
    return templates.TemplateResponse("privacy.html", {"request": request})
```

8. Import Optional from typing and Depends from fastapi if not already imported.
  </action>
  <verify>
```bash
cd ra-tracker && python -c "
from ra_tracker.web.routes import router
routes = [r.path for r in router.routes]
assert '/login' in routes, 'Missing /login route'
assert '/register' in routes, 'Missing /register route'
assert '/logout' in routes, 'Missing /logout route'
assert '/privacy' in routes, 'Missing /privacy route'
print('All auth routes present!')
"
```
  </verify>
  <done>Routes added: GET/POST /login, GET/POST /register, POST /logout, GET /privacy. Login shows error on invalid credentials. Registration validates consent and password length, handles duplicate emails.</done>
</task>

<task type="auto">
  <name>Task 2: Create authentication templates with Tailwind CSS</name>
  <files>ra-tracker/ra_tracker/web/templates/login.html, ra-tracker/ra_tracker/web/templates/register.html, ra-tracker/ra_tracker/web/templates/privacy.html</files>
  <action>
Note: These templates extend base.html which still uses the old CSS. Use inline Tailwind classes that will work once base.html is migrated in Plan 03, but also include any critical styles needed for functionality now.

**Create login.html:**
```html
{% extends "base.html" %}

{% block title %}Login - RA Tracker{% endblock %}

{% block content %}
<div style="max-width: 400px; margin: 2rem auto;">
    <h1 style="text-align: center; margin-bottom: 1.5rem;">Login</h1>

    {% if error %}
    <div class="alert" style="background: rgba(233, 69, 96, 0.2); border: 1px solid var(--accent); padding: 1rem; border-radius: 4px; margin-bottom: 1rem;">
        {{ error }}
    </div>
    {% endif %}

    <div class="card">
        <form method="post" action="/login">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" class="form-control"
                       value="{{ email or '' }}" required autofocus
                       style="min-height: 44px;">
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <div style="position: relative;">
                    <input type="password" id="password" name="password" class="form-control"
                           required style="min-height: 44px; padding-right: 70px;">
                    <button type="button" onclick="togglePassword()"
                            style="position: absolute; right: 8px; top: 50%; transform: translateY(-50%);
                                   background: none; border: none; color: var(--text-muted); cursor: pointer;
                                   padding: 8px;">
                        Show
                    </button>
                </div>
            </div>

            <button type="submit" class="btn btn-primary"
                    style="width: 100%; min-height: 44px; margin-top: 1rem;">
                Login
            </button>
        </form>

        <p style="text-align: center; margin-top: 1.5rem; color: var(--text-muted);">
            Don't have an account?
            <a href="/register" style="color: var(--accent);">Register</a>
        </p>
    </div>
</div>

<script>
function togglePassword() {
    const pwd = document.getElementById('password');
    const btn = event.target;
    if (pwd.type === 'password') {
        pwd.type = 'text';
        btn.textContent = 'Hide';
    } else {
        pwd.type = 'password';
        btn.textContent = 'Show';
    }
}
</script>
{% endblock %}
```

**Create register.html:**
```html
{% extends "base.html" %}

{% block title %}Register - RA Tracker{% endblock %}

{% block content %}
<div style="max-width: 400px; margin: 2rem auto;">
    <h1 style="text-align: center; margin-bottom: 1.5rem;">Create Account</h1>

    <div class="card">
        <form method="post" action="/register" id="register-form">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" class="form-control"
                       value="{{ email or '' }}" required autofocus
                       style="min-height: 44px;">
                {% if errors and errors.email %}
                <small style="color: var(--accent);">{{ errors.email }}</small>
                {% endif %}
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <div style="position: relative;">
                    <input type="password" id="password" name="password" class="form-control"
                           required minlength="8"
                           style="min-height: 44px; padding-right: 70px;">
                    <button type="button" onclick="togglePassword()"
                            style="position: absolute; right: 8px; top: 50%; transform: translateY(-50%);
                                   background: none; border: none; color: var(--text-muted); cursor: pointer;
                                   padding: 8px;">
                        Show
                    </button>
                </div>
                <small class="text-muted">Minimum 8 characters</small>
                {% if errors and errors.password %}
                <br><small style="color: var(--accent);">{{ errors.password }}</small>
                {% endif %}
            </div>

            <div class="form-group">
                <label for="display_name">Display Name</label>
                <input type="text" id="display_name" name="display_name" class="form-control"
                       value="{{ display_name or '' }}" required
                       style="min-height: 44px;">
                <small class="text-muted">Shown in the navigation bar</small>
            </div>

            <!-- CONSENT CHECKBOX - MUST NOT BE PRE-TICKED (GDPR) -->
            <div class="form-group" style="margin-top: 1.5rem;">
                <div style="display: flex; align-items: flex-start; gap: 12px;">
                    <input type="checkbox" id="consent" name="consent" value="yes" required
                           style="width: 20px; height: 20px; min-width: 20px; margin-top: 2px; cursor: pointer;">
                    <label for="consent" style="font-size: 0.9rem; cursor: pointer;">
                        I have read and agree to the
                        <a href="/privacy" target="_blank" style="color: var(--accent); text-decoration: underline;">Privacy Policy</a>
                    </label>
                </div>
                {% if errors and errors.consent %}
                <small style="color: var(--accent); display: block; margin-top: 0.5rem;">{{ errors.consent }}</small>
                {% endif %}
            </div>

            <button type="submit" id="submit-btn" class="btn btn-primary"
                    style="width: 100%; min-height: 44px; margin-top: 1rem;">
                Create Account
            </button>
        </form>

        <p style="text-align: center; margin-top: 1.5rem; color: var(--text-muted);">
            Already have an account?
            <a href="/login" style="color: var(--accent);">Login</a>
        </p>
    </div>
</div>

<script>
function togglePassword() {
    const pwd = document.getElementById('password');
    const btn = event.target;
    if (pwd.type === 'password') {
        pwd.type = 'text';
        btn.textContent = 'Hide';
    } else {
        pwd.type = 'password';
        btn.textContent = 'Show';
    }
}
</script>
{% endblock %}
```

**Create privacy.html:**
```html
{% extends "base.html" %}

{% block title %}Privacy Policy - RA Tracker{% endblock %}

{% block content %}
<div style="max-width: 700px; margin: 0 auto;">
    <h1>Privacy Policy</h1>
    <p class="text-muted" style="margin-bottom: 2rem;">Last updated: January 2026</p>

    <div class="card" style="line-height: 1.8;">
        <h2 style="margin-top: 0;">What Data We Collect</h2>
        <p>When you use RA Tracker, we collect:</p>
        <ul style="margin-left: 1.5rem; margin-bottom: 1.5rem;">
            <li><strong>Email address</strong> - Used for login and optional email notifications</li>
            <li><strong>Display name</strong> - Shown in the navigation bar</li>
            <li><strong>Password</strong> - Stored securely using Argon2id hashing (we never see your actual password)</li>
            <li><strong>Session cookies</strong> - Used to keep you logged in</li>
            <li><strong>Telegram chat ID</strong> (optional) - Only if you link Telegram for notifications</li>
            <li><strong>Tracking rules</strong> - Artists, venues, and promoters you choose to track</li>
        </ul>

        <h2>How Your Data Is Stored</h2>
        <ul style="margin-left: 1.5rem; margin-bottom: 1.5rem;">
            <li><strong>Passwords</strong> are hashed using Argon2id, the algorithm recommended by OWASP for password storage. We cannot see or recover your password.</li>
            <li><strong>Sessions</strong> use secure httponly cookies that cannot be accessed by JavaScript.</li>
            <li><strong>All data</strong> is stored in a SQLite database on the server.</li>
        </ul>

        <h2>Data Retention</h2>
        <ul style="margin-left: 1.5rem; margin-bottom: 1.5rem;">
            <li>Your account and tracking rules are kept until you delete them</li>
            <li>Sessions expire after 30 days of inactivity</li>
            <li>Event data is refreshed periodically and old events are removed</li>
        </ul>

        <h2>Third-Party Services</h2>
        <p>RA Tracker interacts with:</p>
        <ul style="margin-left: 1.5rem; margin-bottom: 1.5rem;">
            <li><strong>Resident Advisor (ra.co)</strong> - We fetch public event data from RA</li>
            <li><strong>Telegram</strong> (optional) - If you enable Telegram notifications, we send messages via the Telegram Bot API using your chat ID</li>
        </ul>
        <p>We do not sell or share your personal data with any other third parties.</p>

        <h2>Your Rights</h2>
        <p>You can:</p>
        <ul style="margin-left: 1.5rem; margin-bottom: 1.5rem;">
            <li>View and update your tracking rules at any time</li>
            <li>Disable notifications by unlinking Telegram or toggling email off</li>
            <li>Request deletion of your account (contact the administrator)</li>
        </ul>

        <h2>Contact</h2>
        <p>For questions about this privacy policy or your data, contact the site administrator.</p>
    </div>

    <p style="text-align: center; margin-top: 2rem;">
        <a href="/register" class="btn btn-primary">Back to Registration</a>
    </p>
</div>
{% endblock %}
```

Key implementation notes:
- All form inputs have `min-height: 44px` for touch targets (WCAG AAA)
- Consent checkbox is NOT pre-ticked (GDPR requirement)
- Show/hide password toggle for usability
- Error messages displayed inline
- Privacy Policy link opens in new tab from register form
  </action>
  <verify>
```bash
# Verify templates exist
ls -la "ra-tracker/ra_tracker/web/templates/login.html" "ra-tracker/ra_tracker/web/templates/register.html" "ra-tracker/ra_tracker/web/templates/privacy.html"

# Verify consent checkbox is NOT pre-ticked
grep -q 'checked' ra-tracker/ra_tracker/web/templates/register.html && echo "ERROR: Checkbox should not be pre-ticked" || echo "OK: Checkbox not pre-ticked"

# Verify privacy link exists in register
grep -q '/privacy' ra-tracker/ra_tracker/web/templates/register.html && echo "OK: Privacy link in register" || echo "ERROR: Missing privacy link"
```
  </verify>
  <done>Three templates created: login.html with email/password form, register.html with consent checkbox (unticked) and privacy link, privacy.html with data collection explanation. All inputs have 44px min-height for mobile touch targets.</done>
</task>

</tasks>

<verification>
Run the app and test manually:

```bash
# Start the app (if not running)
cd ra-tracker && python -m ra_tracker.main &

# Test routes exist
curl -s http://localhost:8080/login | grep -q "Login" && echo "Login page OK"
curl -s http://localhost:8080/register | grep -q "Create Account" && echo "Register page OK"
curl -s http://localhost:8080/privacy | grep -q "Privacy Policy" && echo "Privacy page OK"

# Test registration (should fail without consent)
curl -X POST http://localhost:8080/register \
  -d "email=test@example.com&password=testpass123&display_name=Test" \
  -v 2>&1 | grep -q "Privacy Policy" && echo "Consent required OK"
```
</verification>

<success_criteria>
1. GET /login shows login form
2. POST /login with valid credentials creates session and redirects to /
3. POST /login with invalid credentials shows error, does not redirect
4. GET /register shows registration form with unticked consent checkbox
5. POST /register without consent checkbox returns validation error
6. POST /register with valid data creates user, creates session, redirects to /
7. POST /register with duplicate email shows error
8. POST /logout clears session and cookie, redirects to /login
9. GET /privacy shows privacy policy content
10. All form inputs have minimum 44px height for mobile
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
