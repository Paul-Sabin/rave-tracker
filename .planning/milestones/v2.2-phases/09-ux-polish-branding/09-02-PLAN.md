---
phase: 09-ux-polish-branding
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - ra-tracker/ra_tracker/web/templates/dashboard.html
  - ra-tracker/ra_tracker/web/templates/rules.html
  - ra-tracker/ra_tracker/web/routes.py
autonomous: true

must_haves:
  truths:
    - "User without a region set sees a prompt to select region when visiting rules page"
    - "Region prompt suggests Berlin as the default option"
    - "Dashboard area toggle says 'Global events' instead of 'All Areas'"
    - "Dashboard area toggle says 'Local only' instead of '{region} only'"
    - "Legacy admin welcome banner is removed from dashboard"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/templates/dashboard.html"
      provides: "Updated toggle labels and no legacy banner"
      contains: "Global events"
    - path: "ra-tracker/ra_tracker/web/templates/rules.html"
      provides: "Region prompt for users without region set"
      contains: "Berlin"
    - path: "ra-tracker/ra_tracker/web/routes.py"
      provides: "Region config passed to rules template"
      contains: "has_local_area"
  key_links:
    - from: "ra-tracker/ra_tracker/web/routes.py"
      to: "ra-tracker/ra_tracker/web/templates/rules.html"
      via: "has_local_area template variable"
      pattern: "has_local_area"
    - from: "ra-tracker/ra_tracker/web/templates/dashboard.html"
      to: "filter buttons"
      via: "data-filter attribute and button text"
      pattern: "Global events"
---

<objective>
Improve dashboard UX with clearer toggle labels, remove legacy banner, and add region selection prompt on rules page.

Purpose: New users get guided to set their region before creating rules (ensuring local filtering works). Existing users see clearer "Global events" / "Local only" toggle labels. Legacy admin banner noise is removed.
Output: Updated dashboard.html, rules.html, and routes.py with UX improvements.
</objective>

<execution_context>
@C:/Users/psabi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/psabi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-polish-branding/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard toggle labels and remove legacy banner</name>
  <files>
    ra-tracker/ra_tracker/web/templates/dashboard.html
  </files>
  <action>
    Make two changes to dashboard.html:

    1. **Remove legacy admin welcome banner** (UX-03):
       Delete the entire "Legacy Data Welcome Message" block (lines 20-37 in the current file):
       ```
       <!-- Legacy Data Welcome Message -->
       {% if legacy_data and (legacy_data.rules > 0 or legacy_data.notifications > 0) %}
       ...entire block...
       {% endif %}
       ```
       This banner showed "Welcome! X rules and Y notifications from the previous setup have been assigned to your account." It was a one-time migration message from v1.0->v2.0 that is no longer relevant.

    2. **Update area filter toggle labels** (UX-02):
       In the filter-toggles section (around line 83), change the two location filter buttons:
       - Change `>All Areas</button>` to `>Global events</button>`
       - Change `>{{ local_area_name }} only</button>` to `>Local only</button>`

       The "Global events" button uses data-filter="all" (unchanged).
       The "Local only" button uses data-filter="local" (unchanged).
       The data-area-id attribute remains unchanged.

       The final HTML for the location filter should be:
       ```html
       <div class="filter-group location-filter">
           <button class="filter-btn filter-location active" data-filter="all">Global events</button>
           {% if has_local_area %}
           <button class="filter-btn filter-location" data-filter="local" data-area-id="{{ local_area_id }}">Local only</button>
           {% endif %}
       </div>
       ```

    Do NOT change any JavaScript logic -- the filter-location click handlers and data attributes remain the same. Only the visible button text changes.
  </action>
  <verify>
    Run: `grep "Legacy Data Welcome" ra-tracker/ra_tracker/web/templates/dashboard.html` -- should return zero matches.
    Run: `grep "legacy_data" ra-tracker/ra_tracker/web/templates/dashboard.html` -- should return zero matches.
    Run: `grep "All Areas" ra-tracker/ra_tracker/web/templates/dashboard.html` -- should return zero matches.
    Run: `grep "Global events" ra-tracker/ra_tracker/web/templates/dashboard.html` -- should return 1 match.
    Run: `grep "Local only" ra-tracker/ra_tracker/web/templates/dashboard.html` -- should return 1 match.
  </verify>
  <done>Dashboard shows "Global events" and "Local only" toggle labels. Legacy admin welcome banner is completely removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add region prompt on rules page for users without region</name>
  <files>
    ra-tracker/ra_tracker/web/templates/rules.html
    ra-tracker/ra_tracker/web/routes.py
  </files>
  <action>
    Add a region selection prompt to the rules page that appears when the user has no region configured (UX-01).

    1. **routes.py** -- Update the `rules_page` route (around line 101) to pass region config to the template:
       After getting rules from the database, add config lookup and pass to template context:
       ```python
       config = get_config()
       ```
       Add to the template context dict:
       ```python
       "has_local_area": bool(config.user.local_area_id),
       "local_area_name": config.user.local_area_name,
       ```
       The `get_config` import is already at the top of routes.py.

    2. **rules.html** -- Add a region prompt banner BEFORE the search card, shown only when the user has no region set AND has no rules yet. Insert right after `<h1>Tracking Rules</h1>`:

       ```html
       {% if not has_local_area %}
       <div class="card" style="border-color: #ffc107; background: rgba(255, 193, 7, 0.1);">
           <div style="display: flex; align-items: flex-start; gap: 1rem;">
               <div style="font-size: 1.5rem; flex-shrink: 0;">📍</div>
               <div style="flex: 1;">
                   <p class="font-bold mb-1" style="color: #ffc107;">Set your local region first</p>
                   <p class="text-muted">
                       Choose your city so you can filter events to your area.
                       We suggest starting with <strong>Berlin</strong> if you're unsure.
                   </p>
                   <a href="/settings" class="btn btn-primary mt-2" style="min-height: 44px;">
                       Go to Settings
                   </a>
               </div>
           </div>
       </div>
       {% endif %}
       ```

       This prompt:
       - Appears only when `has_local_area` is False (no region configured)
       - Uses the existing warning color scheme (#ffc107 yellow border)
       - Suggests Berlin as mentioned in the requirement
       - Links to /settings where the Local Area search already exists
       - Uses 44px touch target for the button
       - Does NOT block rule creation -- user can scroll past and create rules without a region

    IMPORTANT: Do NOT add auto-detection logic. Do NOT modify the settings page. The settings page already has a full Local Area search with RA API integration. This task just adds the prompt that guides users there.
  </action>
  <verify>
    Run: `grep "has_local_area" ra-tracker/ra_tracker/web/routes.py` -- should show matches in both dashboard and rules routes.
    Run: `grep "Berlin" ra-tracker/ra_tracker/web/templates/rules.html` -- should return 1 match.
    Run: `grep "Set your local region" ra-tracker/ra_tracker/web/templates/rules.html` -- should return 1 match.
    Run: `grep "has_local_area" ra-tracker/ra_tracker/web/templates/rules.html` -- should return 1 match.
    Verify the rules page route still works by checking the template context includes all required variables.
  </verify>
  <done>Rules page shows region prompt with Berlin suggestion when user has no region set. Prompt links to settings page. Rules page works normally for users with region configured (no prompt shown).</done>
</task>

</tasks>

<verification>
1. Dashboard "Global events" / "Local only" buttons visible and functional (filter logic unchanged)
2. Legacy welcome banner completely absent from dashboard HTML
3. Rules page shows region prompt when no local_area_id configured
4. Rules page hides region prompt when local_area_id is configured
5. Region prompt mentions Berlin as suggestion
6. Region prompt links to /settings (where existing area search works)
7. Application starts without import errors
</verification>

<success_criteria>
- Dashboard filter buttons read "Global events" and "Local only"
- No legacy data welcome banner appears on dashboard
- Users without a region see a yellow prompt on the rules page suggesting they set Berlin
- Users with a region configured see no prompt
- All existing filter functionality works identically (only labels changed)
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-polish-branding/09-02-SUMMARY.md`
</output>
