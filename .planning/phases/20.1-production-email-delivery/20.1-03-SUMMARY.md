---
phase: 20.1-production-email-delivery
plan: 03
subsystem: infra
tags: [brevo, email, railway, documentation, env-config]

# Dependency graph
requires:
  - phase: 20.1-production-email-delivery (plan 01)
    provides: BREVO_API_KEY-gated is_email_configured() and removed SMTP-password fallback in email_sender.py
  - phase: 20.1-production-email-delivery (plan 02)
    provides: routes.py surfaces send failures and writes auth.verification_send_failed / password.reset_send_failed audit events
provides:
  - RAILWAY.md "Production Email Transport" subsection documenting why Railway blocks SMTP and how to switch to Brevo's HTTP API
  - Corrected BREVO_SMTP_PASSWORD bullet (was mislabelled "API key from Brevo dashboard")
  - Documented EMAIL_USE_API, BREVO_API_KEY, SENTRY_DSN, LOGTAIL_SOURCE_TOKEN env vars in RAILWAY.md
  - Troubleshooting runbook for "Emails are not being delivered"
  - EMAIL_USE_API / BREVO_API_KEY placeholder blocks in both .env.example files
affects: [20.1-04 (human sets the real Railway env vars using this doc as the instruction sheet)]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - ra-tracker/RAILWAY.md
    - .env.example
    - ra-tracker/.env.example

key-decisions:
  - "Kept EMAIL_SMTP_*/BREVO_SMTP_* variables documented as the local-dev fallback rather than removing them from RAILWAY.md"
  - "Root .env.example uses an uncommented EMAIL_USE_API=false to make the flag's existence visible; ra-tracker/.env.example matches that file's own empty-value convention for BREVO_API_KEY"
  - "Reworded the ra-tracker/.env.example BREVO_API_KEY comment to spell out the literal variable name so the credential-distinction cross-reference is grep-discoverable, matching the root file's equivalent comment"

requirements-completed: [SC-5]

# Metrics
duration: 8min
completed: 2026-08-23
---

# Phase 20.1 Plan 03: Document Production Email Transport Summary

**RAILWAY.md now explains why Railway blocks outbound SMTP and gives step-by-step instructions plus a troubleshooting runbook for switching to Brevo's HTTP API; both .env.example files carry placeholder EMAIL_USE_API/BREVO_API_KEY blocks.**

## Performance

- **Duration:** ~8 min
- **Completed:** 2026-08-23
- **Tasks:** 2/2 completed
- **Files modified:** 3

## Accomplishments
- Fixed the root-cause-adjacent documentation bug: `BREVO_SMTP_PASSWORD` was labelled "API key from Brevo dashboard" in RAILWAY.md, which is exactly the wrong-credential confusion that contributed to the production incident. That bullet is corrected and `BREVO_API_KEY` is added as its own, distinct Required Secret.
- Added a new `### 3a. Production Email Transport (IMPORTANT)` subsection (ra-tracker/RAILWAY.md lines 64-88) that names the blocked ports, explains the silent-hang failure mode, and gives the four-step Brevo dashboard → Railway env var → redeploy sequence.
- Added `EMAIL_USE_API=true` (with its accepted truthy values) plus the previously-undocumented `SENTRY_DSN` and `LOGTAIL_SOURCE_TOKEN` to the "Email Config (pre-configured)" list (lines 51-59).
- Added a `### Emails are not being delivered` troubleshooting runbook (lines 206-212, immediately under `## Troubleshooting` at line 204) that diagnoses via the audit log and existing error-log strings, and explicitly tells the reader not to revert to SMTP.
- Added matching `EMAIL_USE_API` / `BREVO_API_KEY` placeholder blocks to both `.env.example` (repo root) and `ra-tracker/.env.example`, each cross-referencing `RAILWAY.md` and each other for the credential distinction, using each file's own existing placeholder style.

## Task Commits

Each task was committed atomically:

1. **Task 1: Document the HTTP API transport in RAILWAY.md** - `b9a05f7` (docs)
2. **Task 2: Add the API transport variables to both .env.example files** - `946ce55` (docs)

_No plan-metadata commit yet — this SUMMARY plus STATE/ROADMAP updates are committed separately per the execution protocol._

## Files Created/Modified
- `ra-tracker/RAILWAY.md` - Corrected `BREVO_SMTP_PASSWORD` bullet, added `BREVO_API_KEY` to Required Secrets (lines 43-44); added `EMAIL_USE_API`, `SENTRY_DSN`, `LOGTAIL_SOURCE_TOKEN` to Email Config (lines 51-59); new `### 3a. Production Email Transport` subsection (lines 64-88), inserted before `### 4. Deploy Multiple Services` (now line 90); new `### Emails are not being delivered` troubleshooting entry (lines 206-212), inserted directly under `## Troubleshooting` (line 204)
- `.env.example` - Added `EMAIL_USE_API=false` and `BREVO_API_KEY=your-brevo-http-api-key` with a comment block cross-referencing `ra-tracker/RAILWAY.md`, inserted after the existing `BREVO_SMTP_PASSWORD` line
- `ra-tracker/.env.example` - Added `EMAIL_USE_API=false` and `BREVO_API_KEY=` (empty-value style matching this file's convention) with a comment block cross-referencing `RAILWAY.md`, inserted after the existing `BREVO_SMTP_PASSWORD=` line, before `# Sender email address`

## Decisions Made
- Kept the SMTP variables and their descriptions in RAILWAY.md rather than deleting them — SMTP remains the documented local-development path; only production is required to switch to `EMAIL_USE_API=true`.
- Documented `SENTRY_DSN` and `LOGTAIL_SOURCE_TOKEN` even though they were out of this plan's original credential-fix scope, because RESEARCH.md's Open Question 1 (whether these are actually set in Railway) can only be checked in Plan 04 if they are documented here first. This falls under Rule 2 (auto-add missing critical functionality it's the minimum documentation needed for the next plan's checkpoint) rather than scope creep.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `ra-tracker/.env.example` comment didn't literally contain "BREVO_API_KEY", failing the plan's own acceptance criterion of 2 occurrences**
- **Found during:** Task 2 verification
- **Issue:** The plan's suggested comment text ("Brevo HTTP API key. NOT the same as BREVO_SMTP_PASSWORD above:") describes the credential in prose but never spells out the literal env var name `BREVO_API_KEY`, so `grep -c "BREVO_API_KEY" ra-tracker/.env.example` returned 1, not the 2 the plan's acceptance criteria required (and not matching the root `.env.example`, whose equivalent comment does contain the literal name).
- **Fix:** Reworded the comment's first line to `# BREVO_API_KEY - NOT the same as BREVO_SMTP_PASSWORD above:`, keeping the same meaning while making the variable name grep-discoverable, consistent with the root file's comment style.
- **Files modified:** `ra-tracker/.env.example`
- **Verification:** `grep -c "BREVO_API_KEY" ra-tracker/.env.example` now returns 2.
- **Committed in:** `946ce55` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1)
**Impact on plan:** Minor wording fix only; no scope creep, no behavioural change, no credential value involved.

## Issues Encountered
None.

## User Setup Required
None yet from this plan — this plan is documentation-only. Plan 20.1-04 is the human-action checkpoint that follows the new `### 3a. Production Email Transport` subsection in `ra-tracker/RAILWAY.md` (lines 64-88) step by step: create the Brevo HTTP API key, set `BREVO_API_KEY` and `EMAIL_USE_API=true` on both the web and scheduler Railway services, redeploy, then verify against `https://ravetracker.whotrustswho.com`.

## Next Phase Readiness
RAILWAY.md is now the authoritative, accurate instruction sheet Plan 04 needs: it names the exact dashboard path for the HTTP API key, states the two variables required together, and gives a troubleshooting runbook if the human-verify checkpoint in Plan 04 fails. No blockers for Plan 04.

---
*Phase: 20.1-production-email-delivery*
*Completed: 2026-08-23*

## Self-Check: PASSED

All files (`ra-tracker/RAILWAY.md`, `.env.example`, `ra-tracker/.env.example`, this SUMMARY) confirmed present on disk. Both commit hashes (`b9a05f7`, `946ce55`) confirmed present in `git log`.
