---
phase: 20.1-production-email-delivery
verified: 2026-08-24T05:32:40Z
status: human_needed
score: 6/6 roadmap success criteria verified (code + production), 1 production-specific confirmation item outstanding
overrides_applied: 0
human_verification:
  - test: "Confirm the production audit log directly distinguishes a successful verification send from a failure, for a real send"
    expected: "https://ravetracker.whotrustswho.com/admin/audit-log?event_type=auth.verification_sent shows a fresh row for a real registration, and https://ravetracker.whotrustswho.com/admin/audit-log?event_type=auth.verification_send_failed shows none for that same registration"
    why_human: "Requires an authenticated admin session against the production app; no admin credentials were used during Plan 04's verification run, and the code-level proof (routes.py branching + test_auth_routes.py tests 7-9) is a proxy, not a direct production observation. ra-tracker/scripts/verify_production_email.py already supports this via RT_ADMIN_EMAIL/RT_ADMIN_PASSWORD env vars, so closing this out is a re-run away, not new work."
---

# Phase 20.1: Production Email Delivery Verification Report

**Phase Goal:** Outbound email works again in production, so new users can complete registration and existing users can reset passwords and receive event notifications; and a failed send is visible instead of being reported as success.
**Verified:** 2026-08-24T05:32:40Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria SC-1..SC-6)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | An email sent by the deployed app arrives at an external address | VERIFIED | Orchestrator-confirmed production e2e run (`verify_production_email.py`, 14/14 checks): verification email delivered from `ravetracker@whotrustswho.com` to an external mail.tm inbox |
| SC-2 | A failed send surfaces an error to the user instead of "Verification email sent!" | VERIFIED | `ra_tracker/web/routes.py` — all 5 call sites (lines ~719, ~793-798, ~872-889, ~926-935, ~1055-1063) branch on `sent`; `tests/test_auth_routes.py` tests 1-6 assert the user-visible text/redirect differs by outcome. Full suite green (44 passed, 0 failed, re-run locally) |
| SC-3 | A failed send is distinguishable in the audit log from a successful one | VERIFIED (code+test level); production-specific instance NOT directly confirmed | `auth.verification_send_failed` (4 occurrences) and `password.reset_send_failed` (1 occurrence) written only on the `else` branch, never alongside the success event; `test_auth_routes.py` tests 7-9 assert mutual exclusivity via `db.get_audit_logs()`. Plan 20.1-04's specific must-have — that the *production* audit log shows this for a real send — was not queried with an admin session (see Human Verification Required) |
| SC-4 | Password reset and event notification emails use the same working transport | VERIFIED | `email_sender.py` — all five senders (`send_notification_email`, `send_verification_email`, `send_password_reset_email`, `send_deletion_confirmation_email`, `send_recovery_confirmation_email`) end in `return _send_via_api(...)`; `notifier.py:14,438` imports and calls the same `send_notification_email`/`is_email_configured`. Password reset confirmed live in production (14/14 e2e run); event-notification delivery shares the identical code path (not separately production-tested, but structurally identical, not a separate implementation) |
| SC-5 | The chosen transport and its environment variables are documented in RAILWAY.md | VERIFIED | `ra-tracker/RAILWAY.md` — `### 3a. Production Email Transport` section (line 64) documents the SMTP block, `EMAIL_USE_API`/`BREVO_API_KEY` (4 and 5 occurrences respectively), the credential distinction ("different credential", line 44), and a troubleshooting runbook (`### Emails are not being delivered`, line 206). Both `.env.example` files carry matching placeholder blocks referencing RAILWAY.md |
| SC-6 | Registration end to end works for a brand-new account without manual database intervention | VERIFIED | Orchestrator-confirmed production e2e run: register → `/verify-email` (no `send_failed`) → verification link → `/login?verified=1` → login → `/`, cleanup via the app's own deletion flow, no manual DB access |

**Score:** 6/6 roadmap success criteria have direct or structural evidence; 1 stricter, plan-level production observation (part of SC-3) remains unconfirmed.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `ra-tracker/ra_tracker/services/email_sender.py` | API-key-only credential selection in `_send_via_api` and `is_email_configured` | VERIFIED | Line 94: `api_key = config.email.api_key`; line 140: same. `grep -c "config.email.api_key or config.email.password"` returns 0. Warning message at lines 97-101 names both env vars and the correct dashboard tab. No `api_key`/`config.email.password` interpolated into any logger call |
| `ra-tracker/tests/test_email_sender.py` | 9 unit tests, ≥120 lines | VERIFIED (WIRED) | 9 `def test_` functions found, 150 lines, 5 `patch.object(email_sender.requests...)` calls, 2 `set_config(None)` teardowns. All 9 pass. Content read directly — genuine assertions on payload shape, call-count, log-scrubbing, not stubs |
| `ra-tracker/ra_tracker/web/routes.py` | Boolean-checked sends, failure audit events, `?send_failed=1` handling | VERIFIED | `auth.verification_send_failed` × 4, `password.reset_send_failed` × 1, `sent = await send_` × 5 (no bare/discarded `await send_*` calls), `/verify-email?send_failed=1` × 2, `send_failed: Optional[str] = None` param present, enumeration-safe text (`"If an account exists with that email..."`) unchanged |
| `ra-tracker/tests/test_auth_routes.py` | Route-level coverage of SC-2/SC-3, ≥130 lines | VERIFIED (WIRED) — minor size note | 9 tests, all pass; 123 lines (plan's `min_lines: 130` target missed by 7 lines — cosmetic, not a functional gap; content read directly and confirmed non-trivial, covers exactly the contractual assertions from the plan including the enumeration-safety test) |
| `ra-tracker/RAILWAY.md` | Production Email Transport section + env var docs + troubleshooting | VERIFIED | All required strings present (see SC-5 row above); misleading old bullet (`API key from Brevo dashboard$`) confirmed removed |
| `.env.example`, `ra-tracker/.env.example` | `EMAIL_USE_API`/`BREVO_API_KEY` placeholder blocks | VERIFIED | Both files updated with each file's own placeholder convention, cross-referencing RAILWAY.md; no real credential values (`xkeysib-`/`xsmtpsib-` grep across tracked files in this phase's diff returns nothing new) |
| `.planning/phases/20.1-production-email-delivery/20.1-04-SUMMARY.md` | Recorded production verification outcome | VERIFIED | Present, records env vars set, SENTRY_DSN/LOGTAIL_SOURCE_TOKEN findings, and explicitly flags the SC-3 production-audit-log item as unconfirmed rather than glossing over it |
| `ra-tracker/scripts/verify_production_email.py` | Repeatable e2e production check | VERIFIED | 390 lines, present, executable; independently run by the orchestrator with 14/14 checks passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `tests/test_email_sender.py` | `email_sender.requests.post` | `patch.object(email_sender.requests, "post", ...)` | WIRED | 5 matches; confirmed by reading file — no network access, no `_send_via_api` patched in tests 1-6 |
| `tests/test_email_sender.py` | `ra_tracker.config.set_config` | fixture `set_config(...)`/`set_config(None)` | WIRED | Fixtures `api_config`/`smtp_password_only_config` set and tear down correctly |
| `routes.py` | `verify_email.html {% if error %}` | `"error"` key in `TemplateResponse` context | WIRED | `verify_email_page` (line 851) and `resend_verification_email` (line 888) both set `context["error"]`/`error=` |
| `routes.py` (register, unverified login) | `GET /verify-email` | `303` redirect with `?send_failed=1` | WIRED | Confirmed in both call sites; `test_registration_send_failure_redirects_with_send_failed_param` asserts the exact `Location` header |
| `tests/test_auth_routes.py` | `ra_tracker.web.routes.send_verification_email` | `monkeypatch.setattr` on the routes-module name | WIRED | Confirmed — patches `"ra_tracker.web.routes.send_verification_email"` / `"ra_tracker.web.routes.send_password_reset_email"`, not the service module |
| `notifier.py` | `email_sender.send_notification_email` / `is_email_configured` | direct import | WIRED | `from ..services.email_sender import send_notification_email, is_email_configured` (notifier.py:14); used at lines 415, 438 |
| Railway environment (`EMAIL_USE_API`, `BREVO_API_KEY`) | `config.py:177-180` | env var override into `EmailConfig` | WIRED (production-confirmed) | Both variables set on web and scheduler services per 20.1-04-SUMMARY; production e2e run passed after a redeploy picked them up |
| Deployed app | Brevo HTTPS API | `_send_via_api` POST to `api.brevo.com/v3/smtp/email` | WIRED (production-confirmed) | Orchestrator-confirmed real mail delivery; SUMMARY documents the actual production incident (Brevo authorised-IPs guard) and its resolution |

### Data-Flow Trace (Level 4)

Not applicable in the classic UI-rendering sense — this phase is backend transport/routing logic, not a component rendering fetched data. The equivalent trace (env var → config → transport → external API → real inbox) is covered under Key Link Verification above and confirmed end-to-end in production.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `cd ra-tracker && ./venv/Scripts/python.exe -m pytest` | `44 passed, 0 failed` (re-run independently during this verification) | PASS |
| No SMTP-password fallback remains | `grep -c "config.email.api_key or config.email.password" ra_tracker/services/email_sender.py` | `0` | PASS |
| All 5 call sites branch on the boolean | `grep -c "sent = await send_" ra_tracker/web/routes.py` | `5` | PASS |
| Enumeration-safe text intact | `grep -n "If an account exists with that email"` present, unaltered | 1 match, unchanged text | PASS |
| No real credential committed | `grep -rnE "xkeysib-|xsmtpsib-"` across RAILWAY.md, .env.example files | No matches (pre-existing historical matches only in an unrelated Phase 10 plan file, not touched by this phase) | PASS |
| HEAD == origin/main, tree clean | `git status --porcelain`, `git rev-parse HEAD`/`origin/main` | Clean; both equal `e9fc8bf` | PASS |
| Production end-to-end (register/verify/login/reset/cleanup) | `ra-tracker/scripts/verify_production_email.py` against `https://ravetracker.whotrustswho.com` | 14/14 passed (independently confirmed by orchestrator prior to this report) | PASS |

### Requirements Coverage

This phase has no REQUIREMENTS.md IDs (urgent insertion, discovered 2026-08-22, per ROADMAP.md). Traceability instead runs through ROADMAP-level Success Criteria SC-1..SC-6, referenced in each plan's `requirements:` frontmatter field. All SC-1..SC-6 are addressed as shown in the Observable Truths table above. `.planning/REQUIREMENTS.md` contains no Phase 20.1 entries — confirmed by grep, no orphaned requirements exist for this phase.

### Anti-Patterns Found

None. Scanned `email_sender.py`, `routes.py`, `RAILWAY.md`, and `verify_production_email.py` for TODO/FIXME/placeholder/not-yet-implemented patterns — no matches.

### Human Verification Required

### 1. Production audit log distinguishes success from failure for a real send

**Test:** As an admin, open `https://ravetracker.whotrustswho.com/admin/audit-log?event_type=auth.verification_sent` after triggering a real verification send (e.g. via the register flow or a resend), then open `.../admin/audit-log?event_type=auth.verification_send_failed` for the same event.
**Expected:** A fresh row appears under `auth.verification_sent` and no row appears under `auth.verification_send_failed` for that same send.
**Why human:** Requires an authenticated admin session against the live production app — no admin credentials were used during Plan 04's verification, so this specific production observation (distinct from the code-level and route-test-level proof, which are both solid) has not been made. `ra-tracker/scripts/verify_production_email.py` already has `RT_ADMIN_EMAIL`/`RT_ADMIN_PASSWORD` support for exactly this check; re-running it with those set would close this out without new engineering work.

### Gaps Summary

No code-level or documentation-level gaps were found. Every artifact claimed in the four plan SUMMARYs was independently re-verified against the actual files on disk (not against the summaries' own claims): the credential fix in `email_sender.py`, the five boolean-checked call sites and two new audit event types in `routes.py`, the RAILWAY.md documentation, both `.env.example` files, and the test suites (`test_email_sender.py`, `test_auth_routes.py`) all match what was claimed, are substantive (not stubs), and are wired into the running application. The full test suite passes at 44/0, the working tree is clean and fully pushed, and the orchestrator's independent production e2e run (14/14) confirms SC-1, SC-2 (implicitly, no failure banner shown), SC-4, SC-5, and SC-6 in the live, deployed app.

The one open item is narrow and explicitly flagged by the executor rather than glossed over: Plan 20.1-04's specific must-have that the *production* audit log be queried directly (via an admin session) to confirm `auth.verification_sent` is present and `auth.verification_send_failed` is absent for a real send. The underlying behavior is proven at the code and route-test level (`test_auth_routes.py` tests 7-9 assert exactly this mutual exclusivity against a real database), so this is a confirmation gap, not an implementation gap — but per the verification brief it must not be marked verified until someone with admin access performs it (or re-runs the existing verification script with admin credentials set).

Two minor, non-blocking notes surfaced during re-verification, neither rising to gap status:
- `tests/test_auth_routes.py` is 123 lines against the plan's `min_lines: 130` target — a cosmetic miss with no effect on coverage; all 9 contractual tests are present and pass.
- `VERIFICATION_SEND_FAILED_MESSAGE` appears 3 times in `routes.py` (not the 4 the plan's acceptance criteria predicted), because the expired-link auto-resend path correctly uses its own distinct message string (documented and justified in 20.1-02-SUMMARY, since `verify_expired.html` has no `error` block). Functionally correct; the plan's own acceptance-criteria count was simply off by one against its own `<action>` code.

---

*Verified: 2026-08-24T05:32:40Z*
*Verifier: Claude (gsd-verifier)*
