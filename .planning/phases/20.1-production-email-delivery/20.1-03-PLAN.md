---
phase: 20.1-production-email-delivery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ra-tracker/RAILWAY.md
  - .env.example
  - ra-tracker/.env.example
autonomous: true
requirements: [SC-5]   # SC-5 = ROADMAP Phase 20.1 Success Criterion 5

must_haves:
  truths:
    - "RAILWAY.md states that Railway blocks outbound SMTP and that the HTTP API is the production transport"
    - "RAILWAY.md lists EMAIL_USE_API and BREVO_API_KEY with where to obtain the key"
    - "The docs state explicitly that BREVO_API_KEY is not the same credential as BREVO_SMTP_PASSWORD"
    - "Both .env.example files show the two new variables with placeholder values only"
    - "No real API key or SMTP password value appears in any tracked file"
  artifacts:
    - path: "ra-tracker/RAILWAY.md"
      provides: "Production email transport section plus EMAIL_USE_API / BREVO_API_KEY env var documentation and a troubleshooting entry"
      contains: "EMAIL_USE_API"
    - path: ".env.example"
      provides: "Commented EMAIL_USE_API / BREVO_API_KEY block for local use"
      contains: "BREVO_API_KEY"
    - path: "ra-tracker/.env.example"
      provides: "Same block in the app-level example file"
      contains: "BREVO_API_KEY"
  key_links:
    - from: "ra-tracker/RAILWAY.md"
      to: "Brevo dashboard Settings > SMTP & API > API Keys"
      via: "explicit instruction naming the dashboard path for generating the HTTP API key"
      pattern: "SMTP & API"
    - from: ".env.example"
      to: "ra-tracker/RAILWAY.md"
      via: "cross-reference comment pointing at RAILWAY.md for the credential distinction"
      pattern: "RAILWAY.md"
---

<objective>
Document the transport switch so the next deploy, the next contributor, and future-you
cannot silently reintroduce the SMTP-only setup that broke production.

Purpose: SC-5 (the chosen transport and its environment variables are documented in
RAILWAY.md). This is also the instruction sheet the human follows in Plan 04, so it must
land before that checkpoint runs.

Output:
- `ra-tracker/RAILWAY.md` - new "Production Email Transport" subsection, two new env vars
  in the existing lists, a corrected `BREVO_SMTP_PASSWORD` description, and a
  troubleshooting entry
- `.env.example` and `ra-tracker/.env.example` - commented placeholder block for the two
  new variables
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-production-email-delivery/20.1-RESEARCH.md
@.planning/phases/20.1-production-email-delivery/20.1-PATTERNS.md
@CLAUDE.md
@ra-tracker/CLAUDE.md

<interfaces>
<!-- There are TWO .env.example files. Both need the block. Verified 2026-08-22. -->
<!-- .env.example (repo root, 37 lines) and ra-tracker/.env.example (app level, ~60 lines) -->

Root `.env.example`, lines 14-16 (insertion anchor - add immediately after line 16):
```bash
# SMTP credentials (Brevo)
BREVO_SMTP_USERNAME=your-smtp-username
BREVO_SMTP_PASSWORD=your-smtp-password
```

`ra-tracker/.env.example`, lines 42-43 (insertion anchor - add after the
`BREVO_SMTP_PASSWORD=` line, before `# Sender email address`):
```bash
# Brevo SMTP password (API key from Brevo dashboard)
BREVO_SMTP_PASSWORD=

# Sender email address
EMAIL_FROM_ADDRESS=ravetracker@whotrustswho.com
```
Note this file's style: one comment line, then `VAR=` with an EMPTY value (unlike the
root file, which uses `your-...` placeholders). Match each file's own convention.

`ra-tracker/RAILWAY.md` structure (219 lines) - section anchors:
- line 36 `### 3. Configure Environment Variables`
- lines 39-44 `**Required Secrets:**` list
- lines 52-57 `**Email Config (pre-configured):**` list
- line 138 `## Monitoring`
- line 174 `## Troubleshooting`
- line 195 `## Security Notes`
The file currently contains no occurrence of `EMAIL_USE_API`, `BREVO_API_KEY`,
`SENTRY_DSN` or `LOGTAIL_SOURCE_TOKEN` (verified by grep).

Current, misleading bullet at line 42 (this wording is part of what caused the incident -
it describes an SMTP credential as an "API key"):
```markdown
- `BREVO_SMTP_PASSWORD` - API key from Brevo dashboard
```

Config code these variables map to (`ra_tracker/config.py:177-180`):
```python
if os.environ.get("EMAIL_USE_API", "").lower() in ("true", "1", "yes"):
    config.email.use_api = True
if os.environ.get("BREVO_API_KEY"):
    config.email.api_key = os.environ["BREVO_API_KEY"]
```
So the accepted truthy spellings are exactly `true`, `1`, `yes` (case-insensitive), and
anything else - including `TRUE ` with a trailing space - leaves SMTP selected.
Document `EMAIL_USE_API=true`.

Observability env vars that exist in code but are undocumented
(`ra_tracker/config.py` ObservabilityConfig): `SENTRY_DSN`, `LOGTAIL_SOURCE_TOKEN`,
`ENVIRONMENT`, `LOG_LEVEL`. RESEARCH.md Open Question 1 asks whether the first two are
actually set in Railway - documenting them here is what makes Plan 04 able to check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the HTTP API transport in RAILWAY.md</name>

  <files>ra-tracker/RAILWAY.md</files>

  <read_first>
    - ra-tracker/RAILWAY.md (lines 30-60 for the env var lists, 170-210 for Troubleshooting and Security Notes)
    - .planning/phases/20.1-production-email-delivery/20.1-RESEARCH.md (Summary and Pitfall 1 and Pitfall 4 - the Railway SMTP-block citation and the credential distinction)
    - .planning/ROADMAP.md (the Phase 20.1 entry - the recorded diagnosis this documents)
    - ra-tracker/ra_tracker/config.py (lines 160-185 - the env var names and accepted truthy values)
  </read_first>

  <action>
Four edits, all in `ra-tracker/RAILWAY.md`. Keep the existing bullet format
`` - `VAR_NAME` - description ``.

**Edit 1 - fix the misleading bullet and add the API key to `**Required Secrets:**`
(currently lines 39-44).** Replace:
```markdown
- `BREVO_SMTP_PASSWORD` - API key from Brevo dashboard
```
with:
```markdown
- `BREVO_SMTP_PASSWORD` - Brevo **SMTP key** (Brevo dashboard > Settings > SMTP & API > SMTP tab). Only used for local development; Railway blocks SMTP (see Production Email Transport below)
- `BREVO_API_KEY` - Brevo **HTTP API key** (Brevo dashboard > Settings > SMTP & API > API Keys tab). Required in production. This is a *different credential* from `BREVO_SMTP_PASSWORD` and the two are not interchangeable
```

**Edit 2 - add the transport flag to `**Email Config (pre-configured):**`
(currently lines 52-57).** Add as the first entry of that list, above
`EMAIL_SMTP_SERVER`:
```markdown
- `EMAIL_USE_API=true` - **Required in production.** Sends mail over Brevo's HTTPS API instead of SMTP. Accepted truthy values: `true`, `1`, `yes`. Any other value falls back to SMTP, which does not work on Railway
```
Then append two more bullets to the same list, so the observability vars are no longer
undocumented:
```markdown
- `SENTRY_DSN` - Optional. When set, `logger.error()` calls (including failed email sends) are captured as Sentry events
- `LOGTAIL_SOURCE_TOKEN` - Optional. When set, the application log stream is shipped to Better Stack
```

**Edit 3 - new subsection immediately after the `### 3. Configure Environment Variables`
block (i.e. directly before `### 4. Deploy Multiple Services` at line 60):**
```markdown
### 3a. Production Email Transport (IMPORTANT)

Railway blocks outbound SMTP (ports 25, 465, 587, 2525) and recommends HTTPS-based
transactional email APIs instead. Sending mail over `smtp-relay.brevo.com:587` from a
Railway service therefore fails - typically by hanging until the worker timeout rather
than raising a clean error, which is why the failures were invisible for a while.

Production must use Brevo's HTTP API:

1. In the Brevo dashboard, go to **Settings > SMTP & API > API Keys** and create a key.
   This is *not* the value in `BREVO_SMTP_PASSWORD` - the SMTP key is on the neighbouring
   **SMTP** tab and Brevo's API rejects it with `401 Unauthorized`.
2. In Railway, set `BREVO_API_KEY` to that key.
3. In Railway, set `EMAIL_USE_API=true`.
4. Redeploy the web service (and the scheduler service, which sends event notifications).

Both variables are required together. With `EMAIL_USE_API=true` and no `BREVO_API_KEY`,
the app deliberately reports email as not configured and sends nothing rather than
authenticating with the wrong credential - notification sends are skipped and the
settings page shows email as unconfigured.

Keep the `EMAIL_SMTP_*` / `BREVO_SMTP_*` variables set: the SMTP path remains available
for local development, where port 587 is not blocked.

**Never** paste a real key into this file, `.env.example`, or any other tracked file.
```

**Edit 4 - add a troubleshooting entry** under `## Troubleshooting` (line 174),
following the formatting of the entries already there:
```markdown
### Emails are not being delivered

1. Confirm `EMAIL_USE_API=true` is set on the service (exact value; `True`/`yes`/`1` also
   work, anything else silently selects SMTP).
2. Confirm `BREVO_API_KEY` is set and is an **API key**, not the SMTP key. A wrong key
   shows up in the logs as `Brevo API error 401: ...`.
3. A missing key logs `Brevo API key not configured - set BREVO_API_KEY` and sends nothing.
4. Check `/admin/audit-log?event_type=auth.verification_send_failed` - a row there means
   the app tried and Brevo refused. No rows and no `auth.verification_sent` rows means the
   send was never attempted (usually a missing key).
5. Do not "fix" this by switching back to SMTP. Railway blocks the ports.
```

Do not restructure the document, do not touch the PostgreSQL, backup, scaling or cost
sections, and do not include any real credential value.
  </action>

  <verify>
    <automated>cd C:/develop/repos/psa-learning/rave-tracker && grep -c "EMAIL_USE_API" ra-tracker/RAILWAY.md && grep -c "BREVO_API_KEY" ra-tracker/RAILWAY.md && grep -n "SMTP & API" ra-tracker/RAILWAY.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "EMAIL_USE_API" ra-tracker/RAILWAY.md` is >= 4
    - `grep -c "BREVO_API_KEY" ra-tracker/RAILWAY.md` is >= 5
    - `grep -n "### 3a. Production Email Transport" ra-tracker/RAILWAY.md` returns 1 line, positioned before the line matching `### 4. Deploy Multiple Services`
    - `grep -n "blocks outbound SMTP" ra-tracker/RAILWAY.md` returns 1 line
    - `grep -n "different credential" ra-tracker/RAILWAY.md` returns 1 line
    - `grep -c "SMTP & API" ra-tracker/RAILWAY.md` is >= 3
    - `grep -n "API key from Brevo dashboard$" ra-tracker/RAILWAY.md` returns nothing (the misleading bullet is gone)
    - `grep -n "SENTRY_DSN\|LOGTAIL_SOURCE_TOKEN" ra-tracker/RAILWAY.md` returns 2 lines
    - `grep -n "### Emails are not being delivered" ra-tracker/RAILWAY.md` returns 1 line, positioned after the line matching `## Troubleshooting`
    - `grep -nE "xkeysib|xsmtpsib" ra-tracker/RAILWAY.md` returns nothing (no real Brevo credential pasted)
    - `git diff --name-only` lists only `ra-tracker/RAILWAY.md`
  </acceptance_criteria>

  <done>
    RAILWAY.md explains why SMTP cannot be used on Railway, documents both new variables
    with their Brevo dashboard locations, corrects the misleading SMTP-password bullet,
    documents the two observability variables, and gives a delivery-failure runbook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the API transport variables to both .env.example files</name>

  <files>.env.example, ra-tracker/.env.example</files>

  <read_first>
    - .env.example (whole file, 37 lines - note it uses `your-...` placeholder values)
    - ra-tracker/.env.example (whole file - note it uses empty values after a comment line)
    - ra-tracker/CLAUDE.md (Configuration Patterns - secrets live in .env, never in config.yaml)
  </read_first>

  <action>
Both files. Match each file's own comment/placeholder style; do not normalise one to the
other.

**Root `.env.example`** - insert immediately after the `BREVO_SMTP_PASSWORD=your-smtp-password`
line (currently line 16), keeping the blank line that follows:
```bash

# Brevo HTTP API transport (REQUIRED in production - Railway blocks outbound SMTP)
# BREVO_API_KEY is a different credential from BREVO_SMTP_PASSWORD above:
# Brevo dashboard > Settings > SMTP & API > API Keys. See ra-tracker/RAILWAY.md.
EMAIL_USE_API=false
BREVO_API_KEY=your-brevo-http-api-key
```
`EMAIL_USE_API=false` uncommented is deliberate: local development uses SMTP, and an
explicit `false` documents the flag's existence better than a commented-out line.

**`ra-tracker/.env.example`** - insert after the `BREVO_SMTP_PASSWORD=` line (currently
line 43), before the `# Sender email address` comment, using this file's empty-value style:
```bash

# Brevo HTTP API transport - set to true in production (Railway blocks outbound SMTP)
EMAIL_USE_API=false

# Brevo HTTP API key. NOT the same as BREVO_SMTP_PASSWORD above:
# Brevo dashboard > Settings > SMTP & API > API Keys. See RAILWAY.md.
BREVO_API_KEY=
```

Placeholders only. Never copy a value out of `ra-tracker/.env`. Do not touch
`ra-tracker/.env` (untracked, real secrets), `config.yaml`, or
`ra-tracker/config.example.yaml` - per `ra-tracker/CLAUDE.md`, secrets never go in YAML.
  </action>

  <verify>
    <automated>cd C:/develop/repos/psa-learning/rave-tracker && grep -n "EMAIL_USE_API\|BREVO_API_KEY" .env.example ra-tracker/.env.example && git diff --name-only</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "BREVO_API_KEY" .env.example` returns 2 (comment + variable) and `grep -c "EMAIL_USE_API" .env.example` returns 1
    - `grep -c "BREVO_API_KEY" ra-tracker/.env.example` returns 2 and `grep -c "EMAIL_USE_API" ra-tracker/.env.example` returns 1
    - `grep -n "^EMAIL_USE_API=false" .env.example ra-tracker/.env.example` returns one line in each file
    - `grep -n "your-brevo-http-api-key" .env.example` returns 1 line; `grep -n "^BREVO_API_KEY=$" ra-tracker/.env.example` returns 1 line
    - `grep -n "RAILWAY.md" .env.example ra-tracker/.env.example` returns one line in each file
    - `grep -nE "xkeysib|xsmtpsib" .env.example ra-tracker/.env.example` returns nothing
    - `git diff -- .env.example ra-tracker/.env.example | grep "^+" | grep -vE "^\+\+\+" | grep -cE "[A-Za-z0-9_-]{32,}"` returns 0 (no long secret-shaped strings added)
    - `git status --porcelain ra-tracker/.env` shows nothing (the real .env was not staged or modified)
    - `cd ra-tracker && ./venv/Scripts/python.exe -m pytest` still reports 0 failures (docs-only change must not affect tests)
  </acceptance_criteria>

  <done>
    Both example env files document `EMAIL_USE_API` and `BREVO_API_KEY` with placeholders
    only, each in its own file's style, cross-referencing RAILWAY.md for the credential
    distinction.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git remote (public GitHub repo) | Anything committed is permanently disclosed; `.env.example` and `RAILWAY.md` are both tracked |
| documentation -> future operator | Wrong or ambiguous docs cause the wrong credential to be pasted into a production env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-02 | Information Disclosure | Real Brevo credential committed into `.env.example` or `RAILWAY.md` | mitigate | Placeholders only (`your-brevo-http-api-key` / empty value); acceptance criteria grep for the `xkeysib`/`xsmtpsib` Brevo prefixes and for any added token-shaped string of 32+ chars in the diff; `ra-tracker/.env` explicitly out of scope and asserted unmodified |
| T-20.1-06 | Spoofing | Operator pastes the SMTP key into `BREVO_API_KEY` because the old docs called it "API key from Brevo dashboard" | mitigate | That bullet is rewritten; both dashboard tabs are named explicitly; the "different credential" warning appears in RAILWAY.md and in both `.env.example` files; the troubleshooting entry names the `401` symptom |
| T-20.1-12 | Information Disclosure | Troubleshooting steps teaching an operator to log or echo the key while debugging | mitigate | The runbook diagnoses via the audit log and the existing log messages only; it never instructs anyone to print, echo or log the key value |
</threat_model>

<verification>
1. `grep -rn "EMAIL_USE_API" ra-tracker/RAILWAY.md .env.example ra-tracker/.env.example`
   returns matches in all three files.
2. `git diff --name-only` lists exactly `ra-tracker/RAILWAY.md`, `.env.example`,
   `ra-tracker/.env.example`.
3. `git diff | grep -iE "xkeysib|xsmtpsib"` returns nothing.
4. `cd ra-tracker && ./venv/Scripts/python.exe -m pytest` reports 0 failures.
</verification>

<success_criteria>
- SC-5 satisfied: RAILWAY.md names the transport, explains why SMTP is unusable on
  Railway, and documents `EMAIL_USE_API` plus `BREVO_API_KEY` including where the key
  comes from and that it differs from the SMTP password.
- Both `.env.example` files carry the same information with placeholder values.
- The misleading `BREVO_SMTP_PASSWORD - API key from Brevo dashboard` wording is gone.
- No real credential enters version control.
</success_criteria>

<output>
After completion, create
`.planning/phases/20.1-production-email-delivery/20.1-03-SUMMARY.md`.
Record: which sections of RAILWAY.md were added or changed (with final line numbers), and
note that Plan 04's human task follows the new `### 3a. Production Email Transport`
subsection step by step.
</output>
