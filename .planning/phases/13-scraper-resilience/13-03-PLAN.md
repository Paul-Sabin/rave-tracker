---
phase: 13-scraper-resilience
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - ra-tracker/ra_tracker/web/admin.py
  - ra-tracker/ra_tracker/web/routes.py
  - ra-tracker/ra_tracker/web/templates/dashboard.html
  - ra-tracker/ra_tracker/web/templates/admin/scraper_status.html
autonomous: false

must_haves:
  truths:
    - "Regular users cannot trigger event fetches (button removed from dashboard)"
    - "Admin can view scraper status panel showing state, last success, error count, recent errors, cooldown timer"
    - "Admin can trigger immediate fetch that bypasses circuit breaker"
    - "Admin scraper status page is accessible from admin navigation"
    - "Existing events display normally regardless of scraper state (silent degradation)"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/routes.py"
      provides: "Fetch endpoint restricted to admin only"
    - path: "ra-tracker/ra_tracker/web/templates/dashboard.html"
      provides: "Dashboard without user-facing fetch button"
    - path: "ra-tracker/ra_tracker/web/admin.py"
      provides: "Scraper status route and force-fetch endpoint"
      contains: "scraper-status"
    - path: "ra-tracker/ra_tracker/web/templates/admin/scraper_status.html"
      provides: "Admin scraper monitoring dashboard template"
      contains: "circuit_breaker"
  key_links:
    - from: "ra-tracker/ra_tracker/web/admin.py"
      to: "ra-tracker/ra_tracker/api/circuit_breaker.py"
      via: "import circuit_breaker for status display"
      pattern: "circuit_breaker.get_status"
    - from: "ra-tracker/ra_tracker/web/admin.py"
      to: "ra-tracker/ra_tracker/database.py"
      via: "get_recent_scraper_errors for error log display"
      pattern: "get_recent_scraper_errors"
    - from: "ra-tracker/ra_tracker/web/admin.py"
      to: "ra-tracker/ra_tracker/scheduler/jobs.py"
      via: "run_fetch_now for admin force fetch"
      pattern: "run_fetch_now"
    - from: "ra-tracker/ra_tracker/web/templates/admin/scraper_status.html"
      to: "ra-tracker/ra_tracker/web/templates/base.html"
      via: "extends base template"
      pattern: "extends.*base.html"
---

<objective>
Remove user-facing fetch controls, add admin scraper monitoring dashboard with status panel and force-fetch capability.

Purpose: Regular users should not be able to trigger fetches (only scheduled and admin). Admin needs visibility into scraper health -- last success, error patterns, circuit breaker state, and the ability to force a fetch when needed.

Output: Dashboard without fetch button for users, admin scraper status page with full monitoring panel.
</objective>

<execution_context>
@C:/Users/psabi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/psabi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-scraper-resilience/13-CONTEXT.md
@.planning/phases/13-scraper-resilience/13-01-SUMMARY.md
@.planning/phases/13-scraper-resilience/13-02-SUMMARY.md
@ra-tracker/ra_tracker/web/admin.py
@ra-tracker/ra_tracker/web/routes.py
@ra-tracker/ra_tracker/web/templates/dashboard.html
@ra-tracker/ra_tracker/web/templates/admin/rules.html
@ra-tracker/ra_tracker/web/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove user fetch access and add admin scraper routes</name>
  <files>
    ra-tracker/ra_tracker/web/routes.py
    ra-tracker/ra_tracker/web/templates/dashboard.html
    ra-tracker/ra_tracker/web/admin.py
  </files>
  <action>
**routes.py modifications:**

1. Remove the `trigger_fetch` endpoint (`POST /actions/fetch-now`) entirely from routes.py. The fetch-now capability will live exclusively on the admin scraper status page. Delete the route function and its import of `run_fetch_now` (but keep `get_scheduler_status` import since settings page still uses it).

2. In the `dashboard` function: Remove `scheduler_status` from the template context. The dashboard should NOT show fetch-related info to regular users per the decision "users see no indication of scraper issues." Remove the `status = get_scheduler_status()` line and the `"scheduler_status": status` entry from the template dict.

**dashboard.html modifications:**

1. Remove the "Actions" card entirely (the card div containing the "Fetch Events Now" button, last fetch time display, and next scheduled fetch display). This is approximately lines 38-56 in the current template.

2. The rest of the dashboard (stats grid, upcoming events, filters) stays unchanged. Events continue to display normally regardless of scraper state (silent degradation per user decision).

**admin.py modifications:**

Add imports:
- `from ..api.circuit_breaker import circuit_breaker`
- `from ..scheduler.jobs import run_fetch_now, get_last_fetch_time, get_next_fetch_time`
- `from fastapi.responses import RedirectResponse`
- `import threading`

Add route `GET /admin/scraper-status`:
```python
@admin_router.get("/scraper-status", response_class=HTMLResponse)
async def scraper_status(request: Request, user: User = Depends(require_admin)):
```
- Get circuit breaker status via `circuit_breaker.get_status()`.
- Get recent errors via `db.get_recent_scraper_errors(limit=10)`.
- Get `last_fetch_time = get_last_fetch_time()`.
- Get `next_fetch_time = get_next_fetch_time()`.
- Map circuit breaker state to display labels: CLOSED="Healthy", OPEN="Down", HALF_OPEN="Recovering".
- Render `admin/scraper_status.html` with all this context.

Add route `POST /admin/scraper/fetch-now`:
```python
@admin_router.post("/scraper/fetch-now")
async def force_fetch(request: Request, user: User = Depends(require_admin)):
```
- Call `circuit_breaker.force_close()` to bypass circuit breaker.
- Run `run_fetch_now()` in a background thread (`threading.Thread(target=run_fetch_now, daemon=True).start()`).
- Redirect to `/admin/scraper-status` with status_code=303.

Update the admin nav links in all admin templates (rules.html, users.html, audit_log.html) to include a "Scraper" link:
```html
<a href="/admin/scraper-status" class="btn btn-secondary">Scraper</a>
```
  </action>
  <verify>
Run `cd C:/CLAUDE/ra-tips/ra-tracker && python -c "
from ra_tracker.web.admin import admin_router
routes = [r.path for r in admin_router.routes]
print('Admin routes:', routes)
assert '/scraper-status' in routes
assert '/scraper/fetch-now' in routes
print('Admin scraper routes registered')
"` to verify routes exist.

Check that `/actions/fetch-now` route is removed from routes.py:
`python -c "from ra_tracker.web.routes import router; paths = [r.path for r in router.routes]; assert '/actions/fetch-now' not in paths; print('User fetch route removed')"`.
  </verify>
  <done>
User-facing fetch button removed from dashboard. Admin scraper-status route serves monitoring page. Admin force-fetch route bypasses circuit breaker. Admin nav includes "Scraper" link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin scraper status template</name>
  <files>
    ra-tracker/ra_tracker/web/templates/admin/scraper_status.html
  </files>
  <action>
Create `admin/scraper_status.html` extending `base.html`. Follow the styling patterns from existing admin templates (admin/rules.html, admin/users.html).

Structure:
1. **Page header**: "Admin: Scraper Status" with subtext.

2. **Admin nav bar**: Same as other admin pages -- Rules, Users, Audit Log, Scraper (with Scraper as active/primary).

3. **Status indicator card**: Large visual indicator of current state.
   - Show state name with color coding:
     - CLOSED/Healthy: green (text-success)
     - HALF_OPEN/Recovering: yellow (#ffc107)
     - OPEN/Down: red (text-danger/accent color)
   - Show last successful fetch time (or "Never" if None).
   - Show next scheduled fetch time.
   - Show error count since last success.
   - If OPEN: show cooldown remaining as human-readable time (e.g., "2h 15m remaining"). Use a `<span id="cooldown-timer">` that updates via JavaScript countdown.

4. **Force Fetch card**:
   - Form with POST to `/admin/scraper/fetch-now`.
   - Include CSRF token hidden input.
   - Button text: "Force Fetch Now" with subtext "(Bypasses circuit breaker)".
   - Style as btn-primary.

5. **Recent Errors table** (last 10 errors):
   - Columns: Time, HTTP Code, Error Type, Message, CB State, Rule Target.
   - If no errors, show "No errors recorded" empty state.
   - Use the `.table` class from base.html.
   - Format timestamps as relative time or absolute datetime.
   - Color-code HTTP codes: 403 in red, 429 in yellow, 5xx in orange.

6. **JavaScript for cooldown timer** (only if state is OPEN):
   - Read cooldown_remaining from a data attribute.
   - Count down every second, updating the display.
   - When reaching 0, show "Cooldown expired - next fetch will probe".

Follow these styling conventions from the codebase:
- Use Tailwind v4 classes where appropriate (the CDN is loaded in base.html).
- Use the custom CSS variables: `--color-bg-dark`, `--color-bg-card`, `--color-accent`, `--color-success`, `--color-border`, `--color-text-muted`.
- Use `.card`, `.btn`, `.table` classes from base.html.
- Use `max-w-4xl mx-auto px-4 py-6` for page wrapper (matches other admin pages).
- Mobile-friendly: table should be responsive (horizontal scroll on small screens).
- 44px min touch targets on all buttons.
  </action>
  <verify>
Verify the template file exists and has the required sections:
`python -c "
with open('ra-tracker/ra_tracker/web/templates/admin/scraper_status.html') as f:
    content = f.read()
assert 'extends' in content and 'base.html' in content
assert 'scraper-status' in content or 'Scraper Status' in content
assert 'fetch-now' in content
assert 'csrf_token' in content
assert 'recent_errors' in content or 'error' in content.lower()
print('Template structure OK')
"`.
  </verify>
  <done>
Admin scraper status template shows circuit breaker state with color-coded indicator, last/next fetch times, error count, force-fetch button with CSRF, recent errors table with HTTP code color coding, and live cooldown countdown timer.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify scraper resilience UI changes</name>
  <action>
Human verifies that the dashboard no longer exposes fetch controls to regular users, and that the admin scraper status page displays correctly with all monitoring elements.
  </action>
  <verify>
1. Visit the main dashboard as a regular user -- confirm the "Fetch Events Now" button and "Actions" card are gone. Events should still display normally.
2. Visit `/admin/scraper-status` as an admin user:
   - Confirm status indicator shows "Healthy" (green) since no failures yet.
   - Confirm "Last successful fetch" shows a time or "Never".
   - Confirm "Recent Errors" section shows table (may be empty).
   - Confirm "Force Fetch Now" button is visible.
3. Click "Force Fetch Now" -- should redirect back to the scraper status page. After a few seconds, last fetch time should update.
4. Check admin navigation bar includes "Scraper" link alongside Rules, Users, Audit Log.
5. Verify existing events on dashboard are unaffected (still showing, no stale warnings).
  </verify>
  <done>
Dashboard fetch button removed for regular users. Admin scraper status page renders correctly with all monitoring elements. Force fetch works. Admin nav includes Scraper link.
  </done>
</task>

</tasks>

<verification>
- Dashboard no longer shows "Fetch Events Now" button for regular users
- Dashboard continues showing events normally (silent degradation)
- Admin can access `/admin/scraper-status` with full monitoring panel
- Admin can force-fetch from the status page
- Admin nav includes "Scraper" link on all admin pages
- All CSRF tokens are included in forms
</verification>

<success_criteria>
- SCRAPE-01 met: Exponential backoff on 403/429/5xx (from Plan 01)
- SCRAPE-02 met: User-Agent rotation (from Plan 01)
- SCRAPE-03 met: Circuit breaker handles extended outages (from Plans 01+02)
- SCRAPE-04 met: Status code logging for monitoring (from Plan 02)
- Success criteria 5 met: App continues serving existing events when scraper is down
- Fetch access restricted to admin only
- Admin has full visibility into scraper health
</success_criteria>

<output>
After completion, create `.planning/phases/13-scraper-resilience/13-03-SUMMARY.md`
</output>
