---
phase: 09-ux-polish-branding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ra-tracker/ra_tracker/database.py
  - ra-tracker/ra_tracker/web/routes.py
  - ra-tracker/ra_tracker/web/templates/settings.html
  - ra-tracker/ra_tracker/scheduler/jobs.py
  - ra-tracker/config.yaml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "New user with no local area configured sees yellow region prompt card on rules page"
    - "Existing admin user retains their Berlin local area after migration"
    - "Each user has independent local area setting stored in database"
    - "Settings page saves local area to user record, not global config"
    - "Dashboard reads local_area from user record for event filtering"
  artifacts:
    - path: "ra-tracker/ra_tracker/database.py"
      provides: "User model with local_area_id and local_area_name fields, migration, update method"
      contains: "local_area_id"
    - path: "ra-tracker/ra_tracker/web/routes.py"
      provides: "Routes reading local_area from user object instead of config"
      contains: "user.local_area_id"
    - path: "ra-tracker/ra_tracker/web/templates/settings.html"
      provides: "Settings form reading local area from user object"
      contains: "user.local_area_id"
  key_links:
    - from: "ra-tracker/ra_tracker/web/routes.py"
      to: "database User object"
      via: "user.local_area_id instead of config.user.local_area_id"
      pattern: "user\\.local_area_id"
    - from: "ra-tracker/ra_tracker/web/routes.py (save_settings)"
      to: "database update_user_local_area"
      via: "db method call instead of config.save()"
      pattern: "update_user_local_area"
---

<objective>
Fix per-user local area storage so the region prompt card appears correctly for users who have not configured their local area.

Purpose: Currently local_area_id/local_area_name are stored in global config.yaml (hardcoded to Berlin). All users share this value, so no user ever sees the yellow "Set your local region" prompt card on the rules page. This needs to be per-user in the database.

Output: Per-user local area columns in users table, routes reading from user object, settings saving to user record.
</objective>

<execution_context>
@C:/Users/psabi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/psabi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/region-prompt-missing.md
@ra-tracker/ra_tracker/database.py
@ra-tracker/ra_tracker/web/routes.py
@ra-tracker/ra_tracker/config.py
@ra-tracker/ra_tracker/web/templates/settings.html
@ra-tracker/ra_tracker/scheduler/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-user local_area columns to database and User model</name>
  <files>ra-tracker/ra_tracker/database.py</files>
  <action>
  1. Add `local_area_id: Optional[int] = None` and `local_area_name: str = ""` fields to the User dataclass (after `email_enabled`, before `created_at`).

  2. Add two new migrations to the MIGRATIONS list (after migration 10):
     - Migration 11: `ALTER TABLE users ADD COLUMN local_area_id INTEGER;`
     - Migration 12: `ALTER TABLE users ADD COLUMN local_area_name TEXT DEFAULT '';`

  3. Update ALL methods that construct User objects from database rows to include the new fields. There are exactly 6 places that do `User(id=row["id"], ...)`:
     - `get_user_by_email` (line ~411)
     - `get_user_by_id` (line ~433)
     - `get_unverified_user_by_email` (line ~516)
     - `get_user_by_telegram_chat_id` (line ~538)
     - `get_users_pending_purge` (line ~762)
     - `get_all_users` (line ~1553)
     For each, add after the `email_enabled` line:
     ```python
     local_area_id=row["local_area_id"] if row["local_area_id"] is not None else None,
     local_area_name=row["local_area_name"] or "",
     ```

  4. Add a new method `update_user_local_area`:
     ```python
     def update_user_local_area(self, user_id: int, local_area_id: Optional[int], local_area_name: str) -> None:
         """Update a user's local area preference."""
         with self.get_connection() as conn:
             conn.execute(
                 "UPDATE users SET local_area_id = ?, local_area_name = ? WHERE id = ?",
                 (local_area_id, local_area_name, user_id)
             )
     ```
     Place this method after `set_user_email_enabled` (around line 567), in the user operations section.

  NOTE: Do NOT add a migration to copy config.yaml Berlin value to existing admin user. The config.yaml value will remain as a fallback for the scheduler's global notification check. Users will set their own area through settings.
  </action>
  <verify>
  Run `python -c "from ra_tracker.database import User; u = User(id=1, email='test@test.com', password_hash='x'); print(u.local_area_id, u.local_area_name)"` from the ra-tracker directory - should print `None ` (None and empty string).
  </verify>
  <done>User dataclass has local_area_id and local_area_name fields. MIGRATIONS list has entries 11 and 12 for the new columns. All 6 User construction sites include the new fields. update_user_local_area method exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update routes and templates to use per-user local area</name>
  <files>ra-tracker/ra_tracker/web/routes.py, ra-tracker/ra_tracker/web/templates/settings.html, ra-tracker/ra_tracker/scheduler/jobs.py</files>
  <action>
  **routes.py changes:**

  1. In `dashboard()` (line ~57-98): Replace `config.user.local_area_id` reads with `user.local_area_id`:
     - Line 61: Change `local_area_id = config.user.local_area_id` to `local_area_id = user.local_area_id`
     - Line 93: Change `"local_area_id": config.user.local_area_id,` to `"local_area_id": user.local_area_id,`
     - Line 94: Change `"local_area_name": config.user.local_area_name,` to `"local_area_name": user.local_area_name,`
     - Line 95: Change `"has_local_area": bool(config.user.local_area_id),` to `"has_local_area": bool(user.local_area_id),`
     - Remove `config = get_config()` from dashboard if it's only used for local_area (check: it is NOT used for anything else in dashboard, so remove line 57 `config = get_config()`).

  2. In `rules_page()` (line ~101-129): Replace config reads with user object:
     - Line 114: Remove `config = get_config()`
     - Line 126: Change `"has_local_area": bool(config.user.local_area_id),` to `"has_local_area": bool(user.local_area_id),`
     - Line 127: Change `"local_area_name": config.user.local_area_name,` to `"local_area_name": user.local_area_name,`

  3. In `save_settings()` (line ~258-285): Save local area to user's database record instead of config:
     - After the existing `config.scheduler.fetch_interval_hours = fetch_interval_hours` line, REPLACE the two config.user lines (280-281) with:
       ```python
       # Update local area settings (per-user, stored in database)
       db = get_db()
       db.update_user_local_area(
           user.id,
           local_area_id if local_area_id else None,
           local_area_name
       )
       ```
     - REMOVE the two lines that set `config.user.local_area_id` and `config.user.local_area_name`.
     - Keep `config.save()` because it still saves telegram/scheduler settings.

  **settings.html changes:**

  4. In the Local Area section of settings.html:
     - Line 174: Change `value="{{ config.user.local_area_id or '' }}"` to `value="{{ user.local_area_id or '' }}"`
     - Line 175: Change `value="{{ config.user.local_area_name }}"` to `value="{{ user.local_area_name }}"`
     - Line 176: Change `{% if config.user.local_area_name %}` to `{% if user.local_area_name %}`
     - Line 178: Change `{{ config.user.local_area_name }}` to `{{ user.local_area_name }}`

  **scheduler/jobs.py - NO CHANGES:**

  5. Leave scheduler/jobs.py as-is. The scheduler's `fetch_and_notify()` function reads `config.user.local_area_id` for notification filtering across all users. This is a system-wide default for notifications, separate from per-user dashboard/rules display. The scheduler already passes local_area_id as a parameter to `should_notify_for_event()`, so this can be refactored to per-user in a future phase if needed. For now, the config.yaml Berlin value serves as the global notification area filter which is correct for a single-instance deployment.

  **config.yaml - NO CHANGES:**

  6. Leave config.yaml as-is. The `user` section still serves as a system-wide default for the scheduler notification filter. Removing it would break the scheduler.
  </action>
  <verify>
  Start the app with `cd ra-tracker && python -m ra_tracker.main` (or however it starts). Register a new user account. Navigate to /rules. The yellow "Set your local region first" card should appear with the Berlin suggestion and "Go to Settings" button. For the existing admin user who previously saved Berlin in settings, they should re-set their local area via settings (since it was only stored in config, not their user record).
  </verify>
  <done>
  - Rules page shows yellow region prompt for users with no local_area_id set (has_local_area=False)
  - Dashboard reads local_area from user object for event filtering
  - Settings page saves local area to user database record via update_user_local_area
  - Settings page displays local area from user object, not global config
  - Scheduler continues to use config.yaml for global notification area filter (unchanged)
  </done>
</task>

</tasks>

<verification>
1. Register a new user -> navigate to /rules -> yellow region prompt card appears
2. Click "Go to Settings" -> search for Berlin -> select it -> save -> navigate to /rules -> yellow card is gone
3. Log in as a different user -> /rules should show yellow card (independent per-user storage)
4. Dashboard "Local only" filter uses the logged-in user's local_area_id, not the global config
5. Application starts without errors (migration runs successfully on existing database)
</verification>

<success_criteria>
- New users without local_area configured see the yellow region prompt card on /rules
- Local area setting is stored per-user in the database (users.local_area_id column)
- Settings save writes to user record, not config.yaml
- Existing admin user needs to re-set their area via settings (one-time action)
- Application starts and runs without errors after migration
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-polish-branding/09-03-SUMMARY.md`
</output>
