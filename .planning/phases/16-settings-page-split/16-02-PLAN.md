---
phase: 16-settings-page-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ra-tracker/ra_tracker/config.py
  - ra-tracker/ra_tracker/web/admin.py
  - ra-tracker/ra_tracker/web/templates/admin/settings.html
autonomous: false
requirements:
  - SETT-04
  - SETT-05
  - SETT-06
  - SETT-07
  - SETT-08
  - SETT-09
  - SETT-10
  - SETT-11

must_haves:
  truths:
    - "A non-admin visiting /admin/settings receives a 403 response"
    - "An admin on /admin/settings can view and save Telegram bot token, admin chat ID, fetch schedule times, event horizon, and notification mode"
    - "The fetch schedule field accepts specific times of day (e.g. 08:00, 20:00), not an interval in hours"
    - "Notification mode toggle shows Upon fetch / Daily digest; Daily digest time field appears only when digest mode is selected"
    - "An admin on /admin/settings sees database path/URL (read-only) and a Test Admin Telegram button"
    - "The /admin/settings nav link appears in existing admin page navigation bars"
  artifacts:
    - path: "ra-tracker/ra_tracker/config.py"
      provides: "Config dataclass with fetch_times list, notification_mode, digest_time fields"
      contains: "fetch_times"
    - path: "ra-tracker/ra_tracker/web/admin.py"
      provides: "GET /admin/settings, POST /admin/settings/save, POST /admin/settings/test-telegram routes"
      exports: ["admin_settings", "save_admin_settings", "test_admin_telegram"]
    - path: "ra-tracker/ra_tracker/web/templates/admin/settings.html"
      provides: "Admin settings UI with all system config fields"
      min_lines: 80
  key_links:
    - from: "ra-tracker/ra_tracker/web/templates/admin/settings.html"
      to: "/admin/settings/save"
      via: "HTML form POST action"
      pattern: "admin/settings/save"
    - from: "ra-tracker/ra_tracker/web/admin.py"
      to: "ra-tracker/ra_tracker/config.py"
      via: "get_config() then config.save()"
      pattern: "config\\.save\\(\\)"
    - from: "ra-tracker/ra_tracker/web/admin.py"
      to: "ra-tracker/ra_tracker/web/auth.py"
      via: "Depends(require_admin)"
      pattern: "require_admin"
---

<objective>
Create /admin/settings — a dedicated system configuration page accessible only to admins.

Purpose: System configuration (Telegram bot, fetch schedule, notification mode) must be separated from personal user settings. This plan creates the new admin page with all required fields, admin-only access control (403 for non-admins), and wires up save/test routes.

Output: New admin/settings.html template; new routes in admin.py; updated config.py with fetch_times, notification_mode, digest_time fields.
</objective>

<execution_context>
@C:/Users/psabi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/psabi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-settings-page-split/16-CONTEXT.md
@ra-tracker/ra_tracker/config.py
@ra-tracker/ra_tracker/web/admin.py
@ra-tracker/ra_tracker/web/templates/admin/scraper_status.html
@ra-tracker/ra_tracker/web/templates/settings.html
@ra-tracker/ra_tracker/web/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend config.py with fetch_times, notification_mode, digest_time</name>
  <files>ra-tracker/ra_tracker/config.py</files>
  <action>
    Modify `SchedulerConfig` dataclass and the `Config.load()` / `Config.save()` methods to support the new fields required by /admin/settings.

    **Update `SchedulerConfig` dataclass:**
    ```python
    @dataclass
    class SchedulerConfig:
        fetch_interval_hours: int = 6          # kept for backward compat; UI removed in Phase 16
        event_horizon_days: int = 30
        fetch_times: list = None               # list of "HH:MM" strings, e.g. ["08:00", "20:00"]
        notification_mode: str = "upon_fetch"  # "upon_fetch" or "daily_digest"
        digest_time: str = "08:00"             # "HH:MM" - when to send daily digest

        def __post_init__(self):
            if self.fetch_times is None:
                self.fetch_times = []
    ```

    **Update `Config.load()` YAML parsing for scheduler section:**
    The existing block `if "scheduler" in data: config.scheduler = SchedulerConfig(**data["scheduler"])` will fail if the yaml has unknown or missing keys. Replace this block with safe field-by-field assignment:
    ```python
    if "scheduler" in data:
        sched_data = data["scheduler"]
        config.scheduler.fetch_interval_hours = sched_data.get("fetch_interval_hours", 6)
        config.scheduler.event_horizon_days = sched_data.get("event_horizon_days", 30)
        config.scheduler.fetch_times = sched_data.get("fetch_times", [])
        config.scheduler.notification_mode = sched_data.get("notification_mode", "upon_fetch")
        config.scheduler.digest_time = sched_data.get("digest_time", "08:00")
    ```
    This approach avoids the `TypeError: __init__() got unexpected keyword argument` when old config.yaml files lack the new keys.

    **Update `Config.save()` to include new scheduler fields:**
    In the `data["scheduler"]` dict, add:
    ```python
    "scheduler": {
        "fetch_interval_hours": self.scheduler.fetch_interval_hours,
        "event_horizon_days": self.scheduler.event_horizon_days,
        "fetch_times": self.scheduler.fetch_times,
        "notification_mode": self.scheduler.notification_mode,
        "digest_time": self.scheduler.digest_time,
    },
    ```

    **Validate the change:**
    The `_validate_required_secrets()` method should not be changed.
    Existing `fetch_interval_hours` field is kept for backward compatibility (scheduler/jobs.py still reads it); the UI for it is simply hidden in Phase 16.
  </action>
  <verify>
    `python -c "from ra_tracker.config import Config; c = Config(); print(c.scheduler.fetch_times, c.scheduler.notification_mode, c.scheduler.digest_time)"` — must print `[] upon_fetch 08:00`
    `python -c "from ra_tracker.config import Config; c = Config.load('config.yaml'); print(c.scheduler)"` — must load without TypeError even if config.yaml lacks new fields.
  </verify>
  <done>Config.load() accepts old config.yaml without TypeError; new fields default correctly; Config.save() writes new fields to yaml.</done>
</task>

<task type="auto">
  <name>Task 2: Add /admin/settings routes to admin.py and create admin/settings.html template</name>
  <files>
    ra-tracker/ra_tracker/web/admin.py
    ra-tracker/ra_tracker/web/templates/admin/settings.html
  </files>
  <action>
    **Part A — admin.py: Add three new routes**

    Add the following imports to admin.py (at the top, alongside existing imports):
    ```python
    from fastapi import Form
    from fastapi.responses import JSONResponse
    from ..config import get_config
    from ..services.notifier import Notifier
    ```

    Add three route handlers to `admin_router`:

    **GET /admin/settings:**
    ```python
    @admin_router.get("/settings", response_class=HTMLResponse)
    async def admin_settings(request: Request, user: User = Depends(require_admin)):
        """Admin system configuration page."""
        templates = get_templates(request)
        config = get_config()

        # Mask bot token for display
        bot_token = config.telegram.bot_token
        if bot_token and len(bot_token) > 10:
            masked_token = bot_token[:5] + "*" * (len(bot_token) - 10) + bot_token[-5:]
        elif bot_token:
            masked_token = "*" * len(bot_token)
        else:
            masked_token = ""

        # Database display value: URL (masked) or path
        db_display = config.database.path
        if config.database.url:
            # Show only up to the @ sign to hide credentials
            url = config.database.url
            at_idx = url.find("@")
            if at_idx != -1:
                db_display = "postgresql://****@" + url[at_idx + 1:]
            else:
                db_display = url

        return templates.TemplateResponse(
            "admin/settings.html",
            {
                "request": request,
                "user": user,
                "csrf_token": getattr(request.state, 'csrf_token', ''),
                "config": config,
                "masked_token": masked_token,
                "db_display": db_display,
                "fetch_times_str": ", ".join(config.scheduler.fetch_times),
            },
        )
    ```

    **POST /admin/settings/save:**
    ```python
    @admin_router.post("/settings/save")
    async def save_admin_settings(
        request: Request,
        user: User = Depends(require_admin),
        bot_token: str = Form(""),
        chat_id: str = Form(""),
        fetch_times_str: str = Form(""),
        event_horizon_days: int = Form(30),
        notification_mode: str = Form("upon_fetch"),
        digest_time: str = Form("08:00"),
    ):
        """Save system configuration. Admin only."""
        config = get_config()

        # Update bot token (only if not masked — i.e., user entered a new value)
        if bot_token and "*" not in bot_token:
            config.telegram.bot_token = bot_token

        if chat_id:
            config.telegram.chat_id = chat_id

        # Parse fetch times: comma-separated "HH:MM" strings, strip whitespace
        # Validate basic HH:MM format; silently discard malformed entries
        import re
        times = []
        for t in fetch_times_str.split(","):
            t = t.strip()
            if re.match(r"^\d{2}:\d{2}$", t):
                times.append(t)
        config.scheduler.fetch_times = times

        config.scheduler.event_horizon_days = event_horizon_days

        # Validate notification_mode value
        if notification_mode in ("upon_fetch", "daily_digest"):
            config.scheduler.notification_mode = notification_mode

        # digest_time: validate HH:MM format
        if re.match(r"^\d{2}:\d{2}$", digest_time):
            config.scheduler.digest_time = digest_time

        config.save()

        return RedirectResponse(url="/admin/settings", status_code=303)
    ```

    **POST /admin/settings/test-telegram:**
    ```python
    @admin_router.post("/settings/test-telegram")
    async def test_admin_telegram(request: Request, user: User = Depends(require_admin)):
        """Send a test Telegram message to admin chat ID. Admin only."""
        config = get_config()
        notifier = Notifier()
        try:
            success = notifier.send_test()
            if success:
                return JSONResponse({"status": "success", "message": "Test message sent!"})
            return JSONResponse({"status": "error", "message": "Failed to send"})
        except Exception as e:
            return JSONResponse({"status": "error", "message": str(e)})
    ```

    **Part B — admin/settings.html template**

    Create `ra-tracker/ra_tracker/web/templates/admin/settings.html` using the same pattern as other admin templates (extends base.html, same admin nav links). Structure:

    ```html
    {% extends "base.html" %}

    {% block title %}Admin - System Settings{% endblock %}

    {% block content %}
    <div class="max-w-4xl mx-auto px-4 py-6">
      <div class="mb-6">
        <h1 class="text-2xl font-bold">Admin: System Settings</h1>
        <p class="text-muted">Configure system-wide Telegram, scheduler, and notification settings.</p>
      </div>

      <!-- Admin nav links (consistent with other admin pages) -->
      <div class="flex gap-4 mb-6 flex-wrap">
        <a href="/admin/rules" class="btn btn-secondary">Rules</a>
        <a href="/admin/users" class="btn btn-secondary">Users</a>
        <a href="/admin/audit-log" class="btn btn-secondary">Audit Log</a>
        <a href="/admin/scraper-status" class="btn btn-secondary">Scraper</a>
        <a href="/admin/settings" class="btn btn-primary">Settings</a>
      </div>

      <form action="/admin/settings/save" method="post">
        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">

        <!-- Telegram Configuration -->
        <div class="card">
          <div class="card-header">
            <span class="card-title">Telegram Configuration</span>
          </div>
          <div class="form-group">
            <label for="bot_token">Bot Token</label>
            <input type="text" id="bot_token" name="bot_token" class="form-control"
                   value="{{ masked_token }}" placeholder="Enter Telegram bot token (leave blank to keep current)">
            <small class="text-muted">Create a bot via @BotFather on Telegram. Stored in config.yaml.</small>
          </div>
          <div class="form-group">
            <label for="chat_id">Admin Chat ID</label>
            <input type="text" id="chat_id" name="chat_id" class="form-control"
                   value="{{ config.telegram.chat_id }}" placeholder="Admin chat or group ID">
            <small class="text-muted">Used for admin system alert messages. Use @userinfobot to find your ID.</small>
          </div>
        </div>

        <!-- Scheduler Settings -->
        <div class="card">
          <div class="card-header">
            <span class="card-title">Fetch Schedule</span>
          </div>
          <div class="form-group">
            <label for="fetch_times_str">Fetch Times</label>
            <input type="text" id="fetch_times_str" name="fetch_times_str" class="form-control"
                   value="{{ fetch_times_str }}" placeholder="e.g. 08:00, 20:00">
            <small class="text-muted">Comma-separated times of day to run fetch (HH:MM, 24h). Example: 08:00, 14:00, 20:00</small>
          </div>
          <div class="form-group">
            <label for="event_horizon_days">Event Horizon (days)</label>
            <input type="number" id="event_horizon_days" name="event_horizon_days" class="form-control"
                   value="{{ config.scheduler.event_horizon_days }}" min="1" max="365">
            <small class="text-muted">How many days ahead to fetch events.</small>
          </div>
        </div>

        <!-- Notification Mode -->
        <div class="card">
          <div class="card-header">
            <span class="card-title">Notification Mode</span>
          </div>
          <div class="form-group">
            <label>Dispatch Mode</label>
            <div style="display: flex; flex-direction: column; gap: 0.75rem; margin-top: 0.5rem;">
              <label style="display: flex; align-items: center; gap: 0.75rem; cursor: pointer;">
                <input type="radio" name="notification_mode" value="upon_fetch"
                       {% if config.scheduler.notification_mode == 'upon_fetch' %}checked{% endif %}
                       onchange="toggleDigestTime(this.value)">
                <div>
                  <span style="font-weight: 600;">Upon fetch completion</span>
                  <span class="text-muted" style="display: block; font-size: 0.85rem;">Notifications sent immediately when a fetch finds new events (default)</span>
                </div>
              </label>
              <label style="display: flex; align-items: center; gap: 0.75rem; cursor: pointer;">
                <input type="radio" name="notification_mode" value="daily_digest"
                       {% if config.scheduler.notification_mode == 'daily_digest' %}checked{% endif %}
                       onchange="toggleDigestTime(this.value)">
                <div>
                  <span style="font-weight: 600;">Daily digest</span>
                  <span class="text-muted" style="display: block; font-size: 0.85rem;">Events are queued and sent in a single batched notification each day</span>
                </div>
              </label>
            </div>
          </div>
          <div class="form-group" id="digest-time-group"
               style="{% if config.scheduler.notification_mode != 'daily_digest' %}display: none;{% endif %}">
            <label for="digest_time">Daily Digest Time</label>
            <input type="text" id="digest_time" name="digest_time" class="form-control"
                   value="{{ config.scheduler.digest_time }}" placeholder="HH:MM" style="max-width: 120px;">
            <small class="text-muted">Time of day to send the daily digest (24h format, e.g. 08:00).</small>
          </div>
        </div>

        <button type="submit" class="btn btn-primary">Save System Settings</button>
      </form>

      <!-- Database Info (read-only) -->
      <div class="card" style="margin-top: 1rem;">
        <div class="card-header">
          <span class="card-title">Database</span>
        </div>
        <p class="text-muted">Connection: <code>{{ db_display }}</code></p>
      </div>

      <!-- Test Admin Telegram -->
      <div class="card">
        <div class="card-header">
          <span class="card-title">Test Admin Telegram</span>
        </div>
        <p class="text-muted mb-2">Send a test message to the configured admin chat ID.</p>
        <button id="test-telegram" class="btn btn-secondary">Send Test Message</button>
        <span id="test-result" class="ml-2"></span>
      </div>
    </div>
    {% endblock %}

    {% block scripts %}
    <script>
    function toggleDigestTime(mode) {
        const group = document.getElementById('digest-time-group');
        group.style.display = mode === 'daily_digest' ? 'block' : 'none';
    }

    document.getElementById('test-telegram').addEventListener('click', async () => {
        const resultSpan = document.getElementById('test-result');
        resultSpan.textContent = 'Sending...';
        resultSpan.className = 'text-muted';

        try {
            const response = await fetch('/admin/settings/test-telegram', { method: 'POST' });
            const data = await response.json();

            if (data.status === 'success') {
                resultSpan.textContent = 'Test message sent!';
                resultSpan.className = 'text-success';
            } else {
                resultSpan.textContent = 'Failed: ' + data.message;
                resultSpan.className = 'text-danger';
            }
        } catch (e) {
            resultSpan.textContent = 'Error: ' + e.message;
            resultSpan.className = 'text-danger';
        }
    });
    </script>
    {% endblock %}
    ```

    **Also update existing admin templates to add "Settings" nav link:**
    In each of these templates, find the admin nav div (the row of `<a href="/admin/..." class="btn btn-secondary">` links) and add `<a href="/admin/settings" class="btn btn-secondary">Settings</a>` to the list:
    - `ra-tracker/ra_tracker/web/templates/admin/scraper_status.html`
    - `ra-tracker/ra_tracker/web/templates/admin/audit_log.html`
    - `ra-tracker/ra_tracker/web/templates/admin/rules.html`
    - `ra-tracker/ra_tracker/web/templates/admin/users.html`

    In `scraper_status.html`, the current nav is: Rules | Users | Audit Log | Scraper (primary). Add Settings before Scraper or after it, with `btn-secondary` class (since /admin/scraper-status already has primary).
  </action>
  <verify>
    `python -c "import ra_tracker.web.admin; import ra_tracker.config"` — no import errors.
    Visit http://localhost:8080/admin/settings as admin — page loads with all sections.
    Visit http://localhost:8080/admin/settings as non-admin (or unauthenticated) — must return 403.
    Submit the form with sample values (bot token unchanged, fetch times "08:00, 20:00", event horizon 90, notification_mode "upon_fetch") — redirects back to /admin/settings without error.
    Switching notification_mode radio to "daily_digest" shows the digest time field; switching back hides it.
    "Test Admin Telegram" button triggers the AJAX call to /admin/settings/test-telegram.
    All existing admin pages (rules, users, audit-log, scraper-status) show a "Settings" nav link.
  </verify>
  <done>
    /admin/settings (GET) renders for admin users; returns 403 for non-admins.
    All system config fields present and functional: Telegram bot token (masked), admin chat ID, fetch times, event horizon, notification mode with daily digest time conditional.
    Database info displayed read-only (path or masked URL).
    Test Admin Telegram AJAX endpoint works.
    Save form persists values to config.yaml via config.save().
    Settings nav link added to all existing admin pages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Phase 16 settings split</name>
  <action>
    Verify the Phase 16 settings split is complete and working end-to-end.
    Start the server: `cd ra-tracker && python -m ra_tracker.main -c config.yaml`

    Test 1 — Regular user on /settings: Log in as non-admin. Expected: only Notification Preferences, Account Security, Delete Account. No Telegram Config card, no Fetch Schedule, no Database card. No admin link.

    Test 2 — Admin user on /settings: Log in as admin. Expected: same three personal sections PLUS "System Administration" card with "Go to Admin Settings" link.

    Test 3 — Access control: Non-admin visiting http://localhost:8080/admin/settings must get 403. Admin visiting must get the full system config page.

    Test 4 — Save system settings: On /admin/settings, set fetch times to "08:00, 20:00", event horizon to 90. Save — redirects back, values persist in form.

    Test 5 — Notification mode toggle: Selecting "Daily digest" radio shows digest time field; switching to "Upon fetch" hides it. Values persist on save.

    Test 6 — Admin nav: All existing admin pages (rules, users, audit-log, scraper-status) show a "Settings" nav link that navigates to /admin/settings.
  </action>
  <verify>All six test scenarios pass as described above.</verify>
  <done>Settings split fully verified: personal /settings shows only user-relevant content; /admin/settings is admin-only with all system config fields functional.</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Post-completion checks:
1. `python -c "import ra_tracker.web.admin; from ra_tracker.config import Config"` — clean import
2. GET /admin/settings with non-admin cookie returns 403
3. GET /admin/settings with admin cookie returns 200 with all config fields
4. POST /admin/settings/save with valid form data returns 303 redirect
5. Config.load() works on old config.yaml that lacks new scheduler fields
6. Config.save() writes fetch_times, notification_mode, digest_time to yaml
7. All four existing admin templates have a "Settings" nav link
</verification>

<success_criteria>
- /admin/settings exists and is admin-only (403 for non-admins)
- All SETT-04 through SETT-11 requirements satisfied: editable Telegram bot token, admin chat ID, fetch schedule times, event horizon, notification mode, daily digest time (conditional), read-only DB info, Test Admin Telegram button
- config.py correctly persists all new fields; load/save is backward compatible with old config.yaml files
- Admin nav updated across all admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/16-settings-page-split/16-02-SUMMARY.md`
</output>
