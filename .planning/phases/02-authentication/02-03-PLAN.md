---
phase: 02-authentication
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ra-tracker/ra_tracker/web/templates/base.html
autonomous: true

must_haves:
  truths:
    - "Navigation works on mobile with hamburger menu"
    - "Navigation shows user display name when logged in"
    - "Logout button visible when logged in"
    - "All tap targets are at least 44px"
    - "Page works at 375px width without horizontal scroll"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/templates/base.html"
      provides: "Tailwind CSS setup, mobile-responsive navigation"
      contains: "cdn.tailwindcss.com"
  key_links:
    - from: "ra-tracker/ra_tracker/web/templates/base.html"
      to: "Tailwind CDN"
      via: "script src"
      pattern: "cdn\\.tailwindcss\\.com"
---

<objective>
Migrate base.html to Tailwind CSS with mobile-first responsive navigation.

Purpose: The existing CSS in base.html has no responsive breakpoints and navigation overflows on mobile. This plan adds Tailwind CSS via CDN, implements a hamburger menu for mobile, and updates the navigation to show user state (logged in/out). Child templates inherit these improvements.

Output: base.html with Tailwind CDN, responsive navigation, user-aware header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-CONTEXT.md

# Prior plan context
@.planning/phases/02-authentication/02-01-SUMMARY.md

# File to modify
@ra-tracker/ra_tracker/web/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite base.html with Tailwind CSS and mobile navigation</name>
  <files>ra-tracker/ra_tracker/web/templates/base.html</files>
  <action>
Completely rewrite base.html to use Tailwind CSS while preserving the dark theme. Keep custom CSS properties for theme colors and add Tailwind for layout/responsiveness.

Key requirements:
- Tailwind CSS v4 via CDN with @theme for custom colors
- Mobile hamburger menu (visible below md breakpoint)
- Desktop nav (visible at md breakpoint and above)
- User state awareness: show display_name + logout when logged in, login/register when not
- All interactive elements have min 44px tap targets
- Container max-width ~1200px centered

Replace the entire base.html with:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}RA Tracker{% endblock %}</title>

    <!-- Tailwind CSS v4 CDN -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style type="text/tailwindcss">
      @theme {
        --color-bg-dark: #1a1a2e;
        --color-bg-card: #16213e;
        --color-accent: #e94560;
        --color-accent-hover: #ff6b6b;
        --color-text: #eeeeee;
        --color-text-muted: #aaaaaa;
        --color-success: #4ecca3;
        --color-border: #2a2a4e;
      }
    </style>
    <style>
        /* Base styles that Tailwind doesn't handle well */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background-color: var(--color-bg-dark);
            color: var(--color-text);
            line-height: 1.6;
        }

        /* Form controls */
        .form-control {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid var(--color-border);
            border-radius: 4px;
            background-color: var(--color-bg-dark);
            color: var(--color-text);
            font-size: 1rem;
            min-height: 44px;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--color-accent);
        }

        select.form-control {
            cursor: pointer;
        }

        /* Buttons */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem 1rem;
            min-height: 44px;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
            border: none;
            font-size: 0.9rem;
            transition: background-color 0.2s;
        }

        .btn-primary {
            background-color: var(--color-accent);
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--color-accent-hover);
        }

        .btn-secondary {
            background-color: var(--color-border);
            color: var(--color-text);
        }

        .btn-secondary:hover {
            background-color: #3a3a5e;
        }

        .btn-danger {
            background-color: #dc3545;
            color: white;
        }

        .btn-danger:hover {
            background-color: #c82333;
        }

        .btn-success {
            background-color: var(--color-success);
            color: var(--color-bg-dark);
        }

        .btn-sm {
            padding: 0.25rem 0.5rem;
            min-height: 36px;
            font-size: 0.8rem;
        }

        /* Cards */
        .card {
            background-color: var(--color-bg-card);
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            border: 1px solid var(--color-border);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
            flex-wrap: wrap;
            gap: 0.5rem;
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: 600;
        }

        /* Stats grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background-color: var(--color-bg-card);
            border-radius: 8px;
            padding: 1.5rem;
            text-align: center;
            border: 1px solid var(--color-border);
        }

        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            color: var(--color-accent);
        }

        .stat-label {
            color: var(--color-text-muted);
            font-size: 0.9rem;
        }

        /* Tables */
        .table {
            width: 100%;
            border-collapse: collapse;
        }

        .table th,
        .table td {
            padding: 0.75rem;
            text-align: left;
            border-bottom: 1px solid var(--color-border);
        }

        .table th {
            color: var(--color-text-muted);
            font-weight: 500;
        }

        /* Badge */
        .badge {
            display: inline-block;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.75rem;
        }

        .badge-success {
            background-color: var(--color-success);
            color: var(--color-bg-dark);
        }

        .badge-warning {
            background-color: #ffc107;
            color: var(--color-bg-dark);
        }

        .badge-danger {
            background-color: var(--color-accent);
            color: white;
        }

        /* Alert */
        .alert {
            padding: 1rem;
            border-radius: 4px;
            margin-bottom: 1rem;
        }

        .alert-info {
            background-color: rgba(78, 204, 163, 0.2);
            border: 1px solid var(--color-success);
        }

        .alert-warning {
            background-color: rgba(255, 193, 7, 0.2);
            border: 1px solid #ffc107;
        }

        /* Empty state */
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--color-text-muted);
        }

        .empty-state-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        /* Utility */
        .form-group {
            margin-bottom: 1rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--color-text-muted);
        }

        .text-muted { color: var(--color-text-muted); }
        .text-success { color: var(--color-success); }
        .text-danger { color: var(--color-accent); }
        .mt-1 { margin-top: 0.5rem; }
        .mt-2 { margin-top: 1rem; }
        .mb-1 { margin-bottom: 0.5rem; }
        .mb-2 { margin-bottom: 1rem; }
    </style>
</head>
<body class="bg-bg-dark text-text">
    <!-- Navigation -->
    <nav class="bg-bg-card border-b border-border">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <!-- Logo -->
                <a href="/" class="text-xl font-bold text-accent">RA Tracker</a>

                <!-- Desktop nav (hidden on mobile) -->
                <div class="hidden md:flex items-center gap-4">
                    <a href="/" class="px-4 py-2 min-h-11 flex items-center rounded hover:bg-accent transition-colors {% if request.url.path == '/' %}bg-accent{% endif %}">Dashboard</a>
                    <a href="/rules" class="px-4 py-2 min-h-11 flex items-center rounded hover:bg-accent transition-colors {% if request.url.path == '/rules' %}bg-accent{% endif %}">Rules</a>
                    <a href="/settings" class="px-4 py-2 min-h-11 flex items-center rounded hover:bg-accent transition-colors {% if request.url.path == '/settings' %}bg-accent{% endif %}">Settings</a>

                    {% if user %}
                    <span class="text-text-muted ml-4">{{ user.display_name }}</span>
                    <form action="/logout" method="post" class="inline">
                        <button type="submit" class="px-4 py-2 min-h-11 flex items-center rounded hover:bg-accent transition-colors">Logout</button>
                    </form>
                    {% else %}
                    <a href="/login" class="px-4 py-2 min-h-11 flex items-center rounded hover:bg-accent transition-colors {% if request.url.path == '/login' %}bg-accent{% endif %}">Login</a>
                    {% endif %}
                </div>

                <!-- Hamburger button (visible on mobile) -->
                <button id="hamburger" class="md:hidden p-2 min-w-11 min-h-11 flex items-center justify-center rounded hover:bg-border transition-colors"
                        onclick="toggleMobileNav()">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                              d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>

            <!-- Mobile nav panel -->
            <div id="mobile-nav" class="hidden md:hidden pb-4 space-y-1">
                <a href="/" class="block px-4 py-3 min-h-11 rounded hover:bg-accent transition-colors {% if request.url.path == '/' %}bg-accent{% endif %}">Dashboard</a>
                <a href="/rules" class="block px-4 py-3 min-h-11 rounded hover:bg-accent transition-colors {% if request.url.path == '/rules' %}bg-accent{% endif %}">Rules</a>
                <a href="/settings" class="block px-4 py-3 min-h-11 rounded hover:bg-accent transition-colors {% if request.url.path == '/settings' %}bg-accent{% endif %}">Settings</a>

                {% if user %}
                <div class="border-t border-border pt-2 mt-2">
                    <span class="block px-4 py-2 text-text-muted">{{ user.display_name }}</span>
                    <form action="/logout" method="post">
                        <button type="submit" class="block w-full text-left px-4 py-3 min-h-11 rounded hover:bg-accent transition-colors">
                            Logout
                        </button>
                    </form>
                </div>
                {% else %}
                <div class="border-t border-border pt-2 mt-2">
                    <a href="/login" class="block px-4 py-3 min-h-11 rounded hover:bg-accent transition-colors">Login</a>
                    <a href="/register" class="block px-4 py-3 min-h-11 rounded hover:bg-accent transition-colors">Register</a>
                </div>
                {% endif %}
            </div>
        </div>
    </nav>

    <!-- Main content -->
    <main class="max-w-7xl mx-auto px-4 py-8">
        {% block content %}{% endblock %}
    </main>

    <script>
    function toggleMobileNav() {
        const nav = document.getElementById('mobile-nav');
        nav.classList.toggle('hidden');
    }

    // Close mobile nav when clicking outside
    document.addEventListener('click', function(e) {
        const nav = document.getElementById('mobile-nav');
        const hamburger = document.getElementById('hamburger');
        if (!nav.contains(e.target) && !hamburger.contains(e.target)) {
            nav.classList.add('hidden');
        }
    });
    </script>

    {% block scripts %}{% endblock %}
</body>
</html>
```

Key changes from original:
- Added Tailwind CSS v4 CDN with @theme for custom colors
- Navigation now has desktop (md:flex) and mobile (hamburger) modes
- All nav links have min-h-11 (44px) for touch targets
- User state shown: display_name + logout when logged in
- Mobile nav panel slides down when hamburger clicked
- Preserved all existing component styles (.card, .btn, .form-control, etc.) for backward compatibility
- Main content area uses max-w-7xl for consistent width
  </action>
  <verify>
```bash
# Verify Tailwind CDN is included
grep -q "cdn.jsdelivr.net.*tailwindcss" ra-tracker/ra_tracker/web/templates/base.html && echo "OK: Tailwind CDN included"

# Verify hamburger button exists
grep -q "hamburger" ra-tracker/ra_tracker/web/templates/base.html && echo "OK: Hamburger button exists"

# Verify user template variable is used
grep -q "{{ user.display_name }}" ra-tracker/ra_tracker/web/templates/base.html && echo "OK: User display_name shown"

# Verify mobile nav exists
grep -q "mobile-nav" ra-tracker/ra_tracker/web/templates/base.html && echo "OK: Mobile nav exists"
```
  </verify>
  <done>base.html rewritten with Tailwind CSS v4 CDN, mobile hamburger navigation, desktop nav at md breakpoint, user state display (display_name + logout), all tap targets 44px minimum. Existing component classes preserved for backward compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Update routes to pass user to templates</name>
  <files>ra-tracker/ra_tracker/web/routes.py</files>
  <action>
Update existing routes (dashboard, rules, settings) to pass `user` to template context so base.html can show login state.

1. Update the imports if not already done:
```python
from .auth import get_current_user
```

2. Update dashboard route to accept optional user:
```python
@router.get("/", response_class=HTMLResponse)
async def dashboard(request: Request, user: Optional[User] = Depends(get_current_user)):
    """Main dashboard showing upcoming events."""
    templates = get_templates(request)
    # ... existing code ...
    return templates.TemplateResponse(
        "dashboard.html",
        {
            "request": request,
            "user": user,  # ADD THIS
            # ... existing context ...
        },
    )
```

3. Update rules_page route:
```python
@router.get("/rules", response_class=HTMLResponse)
async def rules_page(request: Request, user: Optional[User] = Depends(get_current_user)):
    """Rules management page."""
    templates = get_templates(request)
    # ... existing code ...
    return templates.TemplateResponse(
        "rules.html",
        {
            "request": request,
            "user": user,  # ADD THIS
            # ... existing context ...
        },
    )
```

4. Update settings_page route:
```python
@router.get("/settings", response_class=HTMLResponse)
async def settings_page(request: Request, user: Optional[User] = Depends(get_current_user)):
    """Settings page."""
    templates = get_templates(request)
    # ... existing code ...
    return templates.TemplateResponse(
        "settings.html",
        {
            "request": request,
            "user": user,  # ADD THIS
            # ... existing context ...
        },
    )
```

5. Also add user to login_page, register_page, and privacy_page templates (even if they don't require auth, base.html needs it):
```python
# In login_page, register_page:
return templates.TemplateResponse("login.html", {"request": request, "user": user})

# In privacy_page (add user param):
@router.get("/privacy", response_class=HTMLResponse)
async def privacy_page(request: Request, user: Optional[User] = Depends(get_current_user)):
    """Privacy Policy page."""
    templates = get_templates(request)
    return templates.TemplateResponse("privacy.html", {"request": request, "user": user})
```

This ensures every template has access to `user` variable for the navigation.
  </action>
  <verify>
```bash
# Verify user is passed to all templates
grep -c '"user": user' ra-tracker/ra_tracker/web/routes.py
# Should show at least 6 occurrences (dashboard, rules, settings, login, register, privacy)
```
  </verify>
  <done>All routes pass `user` to template context. Navigation shows login state correctly on all pages.</done>
</task>

</tasks>

<verification>
Test mobile navigation works:

1. Start app and open in browser
2. Resize window to mobile width (<768px)
3. Verify hamburger icon appears
4. Click hamburger, verify nav panel opens
5. Verify nav links have adequate tap size
6. Test at 375px width - no horizontal scroll

Test user state display:
1. Visit /login while not logged in - nav shows "Login" link
2. Log in - nav shows display_name and "Logout"
3. Log out - nav shows "Login" link again
</verification>

<success_criteria>
1. Tailwind CSS loaded via CDN (check Network tab)
2. Hamburger menu visible on mobile (<768px)
3. Desktop nav visible on desktop (>=768px)
4. Nav shows user display_name when logged in
5. Nav shows Logout button when logged in
6. Nav shows Login link when not logged in
7. All nav tap targets are at least 44px
8. No horizontal scroll at 375px width
9. Existing component styles (.card, .btn, etc.) still work
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
