---
phase: 03-multi-tenant-access
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - ra-tracker/ra_tracker/web/routes.py
  - ra-tracker/ra_tracker/web/templates/dashboard.html
autonomous: true

must_haves:
  truths:
    - "Dashboard shows only events matching user's rules"
    - "Rules page shows only user's rules"
    - "Adding a rule assigns it to current user"
    - "Rule toggle/delete/mode-change verifies ownership first"
    - "User cannot modify another user's rule"
    - "API endpoints scope data to current user"
    - "Stats on dashboard reflect user's data only"
  artifacts:
    - path: "ra-tracker/ra_tracker/web/routes.py"
      provides: "User-scoped route handlers"
      exports: ["dashboard", "rules_page", "add_rule", "toggle_rule", "delete_rule", "api_add_rule", "api_check_rule"]
    - path: "ra-tracker/ra_tracker/web/templates/dashboard.html"
      provides: "Legacy data welcome message"
      contains: "legacy_count"
  key_links:
    - from: "routes.py dashboard"
      to: "db.get_upcoming_events_for_user"
      via: "pass user.id"
      pattern: "get_upcoming_events_for_user\\(user\\.id\\)"
    - from: "routes.py add_rule"
      to: "db.add_rule"
      via: "pass user_id parameter"
      pattern: "add_rule\\(rule, user_id=user\\.id\\)"
    - from: "routes.py toggle_rule"
      to: "db.get_rule_for_user"
      via: "ownership check before mutation"
      pattern: "get_rule_for_user\\(rule_id, user\\.id\\)"
---

<objective>
Update all routes to scope data access to the logged-in user

Purpose: Implement multi-tenant isolation at the route level by passing user.id to database methods and verifying rule ownership before mutations.

Output: Routes that only show/modify the current user's data, with legacy data welcome message on dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-tenant-access/03-RESEARCH.md
@.planning/phases/03-multi-tenant-access/03-01-SUMMARY.md

# Source files to modify
@ra-tracker/ra_tracker/web/routes.py
@ra-tracker/ra_tracker/web/templates/dashboard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard and rules page to use user-scoped queries</name>
  <files>ra-tracker/ra_tracker/web/routes.py, ra-tracker/ra_tracker/web/templates/dashboard.html</files>
  <action>
**In routes.py:**

1. **Update dashboard() route:**
   - Replace `db.get_upcoming_events()` with `db.get_upcoming_events_for_user(user.id)`
   - Replace `db.get_all_rules()` with `db.get_all_rules(user_id=user.id)`
   - Replace `db.get_stats()` with `db.get_user_stats(user.id)`
   - Add legacy data count: `legacy_data = db.count_legacy_data(user.id)`
   - Pass `legacy_data` to template context

2. **Update rules_page() route:**
   - Replace `db.get_all_rules()` with `db.get_all_rules(user_id=user.id)`
   - User only sees their own rules to manage

**In dashboard.html:**

3. **Add legacy data welcome message:**
   - At the top of the main content (after header, before events)
   - Only show if legacy_data.rules > 0 or legacy_data.notifications > 0
   - Message: "Welcome! X rules and Y notifications from the previous setup have been assigned to your account."
   - Style as an info alert with Tailwind: `bg-blue-50 border-l-4 border-blue-400 p-4 mb-4`
   - Add dismiss button (JavaScript to hide, no backend needed)

Template pattern:
```html
{% if legacy_data and (legacy_data.rules > 0 or legacy_data.notifications > 0) %}
<div id="legacy-alert" class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-4 flex justify-between items-start">
  <div>
    <p class="font-medium text-blue-800">Welcome!</p>
    <p class="text-blue-700">
      {{ legacy_data.rules }} rule{{ 's' if legacy_data.rules != 1 else '' }}
      {% if legacy_data.notifications > 0 %}
      and {{ legacy_data.notifications }} notification{{ 's' if legacy_data.notifications != 1 else '' }}
      {% endif %}
      from the previous setup have been assigned to your account.
    </p>
  </div>
  <button onclick="document.getElementById('legacy-alert').remove()" class="text-blue-400 hover:text-blue-600" aria-label="Dismiss">
    <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/></svg>
  </button>
</div>
{% endif %}
```
  </action>
  <verify>
1. Start app and login
2. Dashboard should show only events matching your rules
3. Rules page should show only your rules
4. Stats (active rules, upcoming events, notifications) should reflect your data
5. If legacy data exists, welcome message appears
  </verify>
  <done>
- Dashboard displays user-scoped events and stats
- Rules page displays user-scoped rules
- Legacy data welcome message shows for users with migrated data
  </done>
</task>

<task type="auto">
  <name>Task 2: Update rule mutations and API endpoints to scope by user</name>
  <files>ra-tracker/ra_tracker/web/routes.py</files>
  <action>
**Update rule creation to assign user_id:**

1. **add_rule() route:**
   - Replace `db.rule_exists(rule_type, target_id)` with `db.rule_exists(rule_type, target_id, user_id=user.id)`
   - Replace `db.add_rule(rule)` with `db.add_rule(rule, user_id=user.id)`

2. **api_add_rule() route:**
   - Same changes as add_rule()
   - Change error message from "Already tracking this artist" to "You are already tracking this"

**Update rule mutations to verify ownership:**

3. **toggle_rule() route:**
   - Replace `db.get_rule(rule_id)` with `db.get_rule_for_user(rule_id, user.id)`
   - If rule is None, return 404 (rule not found OR doesn't belong to user)
   - Security: never reveal if rule exists for another user

4. **set_notify_mode() route:**
   - Replace `db.get_rule(rule_id)` with `db.get_rule_for_user(rule_id, user.id)`
   - Same 404 handling as toggle_rule

5. **delete_rule() route:**
   - Add ownership check: `rule = db.get_rule_for_user(rule_id, user.id)`
   - If rule is None, return 404
   - Only delete if ownership verified

**Update API endpoints to scope by user:**

6. **api_check_rule() route:**
   - Replace `db.rule_exists(rule_type, target_id)` with `db.rule_exists(rule_type, target_id, user_id=user.id)`
   - This allows per-user duplicate checking for the "track" buttons

7. **get_status() route:**
   - Replace `db.get_stats()` with `db.get_user_stats(user.id)`
   - Stats should be user-scoped

Pattern for ownership verification:
```python
@router.post("/rules/{rule_id}/delete")
async def delete_rule(rule_id: int, user: User = Depends(require_auth)):
    """Delete a rule."""
    db = get_db()
    rule = db.get_rule_for_user(rule_id, user.id)
    if not rule:
        raise HTTPException(status_code=404, detail="Rule not found")
    db.delete_rule(rule_id)
    return RedirectResponse(url="/rules", status_code=303)
```
  </action>
  <verify>
1. Add a rule - verify it gets assigned to your user (check database directly)
2. Try to toggle/delete a rule via URL manipulation with wrong rule_id - should get 404
3. API endpoint /api/rules/check should only check against your own rules
4. /api/status should return your stats only
  </verify>
  <done>
- add_rule assigns rule to current user
- rule_exists checks per-user duplicates
- toggle_rule, set_notify_mode, delete_rule verify ownership
- API endpoints scope data to current user
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Start the app: `cd ra-tracker && python -m ra_tracker`
2. Login as user A, add some rules
3. Register user B in a different browser/incognito
4. User B's dashboard should be empty (no rules, no events)
5. User B should not see User A's rules on /rules page
6. User B cannot delete User A's rules via URL manipulation
</verification>

<success_criteria>
1. Dashboard shows only current user's matched events
2. Rules page shows only current user's rules
3. Stats reflect current user's data
4. New rules are assigned to current user
5. Rule mutations verify ownership (404 for wrong user)
6. API endpoints scope data to current user
7. Legacy data message appears for users with migrated data
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-tenant-access/03-02-SUMMARY.md`
</output>
